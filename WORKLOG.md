# 작업 공유 로그

Codex와 Claude Code가 번갈아 작업할 때 변경 맥락을 공유하는 파일입니다.
README는 설치·실행·구조 설명용으로 유지하고, 작업 이력과 인수인계 사항은 이 파일에 기록합니다.

## 기록 규칙

- 코드·설정·배포 관련 변경을 했다면 같은 커밋에서 이 파일도 갱신합니다.
- 최신 기록을 `최근 변경`의 맨 위에 추가합니다.
- 길게 설명하지 말고 변경 내용, 이유, 검증 결과, 남은 일을 적습니다.
- 실제로 확인하지 않은 항목은 완료로 쓰지 않습니다.
- 비밀 키, 비밀번호, SMTP 자격 증명, `service_role` 키는 기록하지 않습니다.
- 자동 생성 데이터만 바뀐 경우에는 사용자 기능에 영향이 있을 때만 기록합니다.

기록 형식:

```md
### YYYY-MM-DD — 작업 제목

- 작업자: Codex 또는 Claude Code
- 변경: 무엇을 바꿨는지
- 이유: 왜 바꿨는지
- 검증: 실제 확인한 내용
- 남은 일: 없으면 `없음`
- 관련 파일: `파일명`
```

## 현재 상태

- 서비스 주소: https://monasset.kr
- 저장소: https://github.com/oliversong25-lang/mon2
- 배포: GitHub Pages, `main` 브랜치
- 구성: 빌드 도구 없는 정적 HTML/CSS/JavaScript
- 인증·계정 저장: Supabase 이메일 로그인과 `user_asset_sessions` 테이블
- 데이터 보호: `auth.uid()` 기준 RLS 정책. 클라이언트에는 공개 가능한 publishable key만 사용
- 이메일 발송: Resend의 `auth.monasset.kr` 발신 도메인 인증 및 Supabase SMTP 연동
- 화면 흐름: 미로그인 사용자는 로그인 화면으로 이동하고, 로그인 후 자산 입력 또는 홈으로 진입
- 공통 내비게이션: 왼쪽 아래 `내 자산` 옆 로그아웃 버튼 제공

## 최근 변경

### 2026-08-14 — 화면 디자인 개편 (전 화면)

- 작업자: Claude Code
- 변경: 강조색을 없애고 조작 가능한 것을 전부 무채색 본지(`--action #f0ede4`)로 바꿨습니다. 배경을 근-검정에서 잉크 그라파이트로 올리고, 활자를 세 역할(Hahmlet 명조 표제 · Pretendard 본문 · IBM Plex Mono 유틸리티)로 나눴습니다. 홈에는 총자산의 억·만·원 단위 조판과 기준일 레일을 넣었습니다. `asset-input.html`이 통째로 복사해 갖고 있던 토큰을 걷어내고 `lib/app.css`를 쓰게 했습니다.
- 이유: 예전 `--brand #5b7cfa`가 하락색 `--neg #5b8dee`와 ΔE 6.1로 사실상 같은 색이라, 버튼·포커스·활성 내비가 "하락"과 구별되지 않았습니다. 색을 상승·하락과 구성에만 남기면 이 화면에서 색이 한 가지만 뜻하게 됩니다.
- 검증: 전체 회귀 테스트 254건 통과. 도넛 6색은 바꾸지 않았고 새 표면(`#1a1c26`)에서 대비 최저 4.30:1로 3:1 기준을 유지합니다(기존 표면 4.36:1). 상단 3열 비율은 1.35:1:1을 실측 확인했고, 폰트 세 종이 모든 화면에서 실제 적용되는 것을 확인했습니다. 1440/1024/768/390px에서 가로 넘침 0px, `prefers-reduced-motion`에서 레일 애니메이션이 꺼지는 것, 포커스 링(2px solid)이 보이는 것도 확인했습니다.
- 남은 일: 도넛 팔레트의 커밋된 CVD 수치(인접쌍 최저 ΔE 8.4)가 재현되지 않습니다. CIEDE2000 + Viénot로 다시 재면 최저값이 1.6이고(보라 `#9085e9` ↔ 파랑 `#3987e5`, 적색맹, 도넛 12시에서 맞닿는 쌍), 3색맹에서는 `#c98500` ↔ `#d55181`이 2.1입니다. 원래 검증이 다른 모델·다른 공식이었을 수 있어 팔레트는 건드리지 않았습니다. 색을 바꾸려면 먼저 어느 방법으로 8.4가 나왔는지 확인해야 합니다.
- 관련 파일: `lib/app.css`, `home.html`, `assets.html`, `asset-input.html`, `indicators.html`, `login.html`, `settings.html`, `index.html`, `scripts/test-home.mjs`, `scripts/lib/test-auth.mjs`

### 2026-08-14 — 거시 일정 확대(금리 결정 → 주요 지표)와 주별·월별 보기

- 작업자: Claude Code
- 변경: `다가오는 일정`의 범위를 금리 결정에서 한국·미국 주요 지표 발표까지 넓혔습니다(24건 → 74건). 일정마다 `category`·`country`·`importance`·`dateBasis`를 붙였고, 컴팩트 카드와 확대 보기 모두에 `가까운 순 · 주별 · 월별` 전환을, 확대 보기에는 국가·중요도 필터를 넣었습니다. 파일은 `scripts/build-macro-calendar.mjs`가 생성합니다 — 날짜는 스크립트 안에 손으로 적혀 있고 외부 API는 부르지 않습니다.
- 이유: 금리 결정만 담은 것은 단계적 결정이었지 목표 상태가 아니었습니다. 일정이 74건이 되면 가까운 순 한 줄 목록으로는 "이번 주에 뭐가 있나"를 볼 수 없어 주·월 묶음이 함께 필요했습니다. 생성기를 둔 것은 이 파일에서 틀리기 쉬운 것이 날짜가 아니라 시간대 환산이기 때문입니다 — 08:30 ET는 한국에서 같은 날, 14:00 ET는 다음 날이고 서머타임 경계에서 또 갈립니다.
- 검증: 전체 회귀 테스트 254건 통과(홈 81건, 신규 9건 포함 · Codex의 IME 수정 위에 재적용). 시간대는 실측으로 확인했습니다 — 08:30 ET는 서머타임 21:30·이후 22:30으로 **같은 날**, 14:00 ET는 03:00·04:00으로 **다음 날**. 서머타임 경계(2026-11-01) 양쪽에서 값이 실제로 갈리는지도 테스트가 봅니다. 주 경계는 8/31(월)과 9/6(일)이 같은 주로 묶이고 9/7이 다음 주로 갈리는 것을 확인했습니다. 상단 3열 행 높이는 변경 전후 모두 327px로 같고(일정 카드 자연 높이 207 → 236, 행보다 작음), 로컬 서버에서 실제 클릭으로 주별 전환·확대 보기·출처 링크를 확인했습니다.
- 남은 일: ISM 공표 달력(ismworld.org)이 로그인을 요구해 열지 못했습니다. ISM 8건과 산업통상부 수출입 동향 4건은 기관이 밝힌 규칙(첫/셋째 영업일, 매월 1일)으로 날짜를 잡고 `dateBasis: "rule"`로 표시했으며 화면에도 `예정` 표시가 붙습니다. 공휴일에 밀릴 수 있으니 달력을 열 수 있게 되면 확정 날짜로 교체해야 합니다. 미 노동통계국(bls.gov)은 자동 조회를 차단(403)해 BLS 일정을 그대로 싣는 페이지에서 확인했고, 갱신할 때는 브라우저로 직접 열어야 합니다. 2027년치는 대부분의 기관이 아직 공표하지 않아 FOMC만 들어 있습니다.
- 관련 파일: `scripts/build-macro-calendar.mjs`, `data/macro-calendar.json`, `home.html`, `scripts/test-home.mjs`, `README.md`

### 2026-08-14 — 경제지표 검색 한글 IME 오류 수정

- 작업자: Codex
- 변경: 경제지표 검색 시 화면 전체를 다시 그리지 않고 결과 목록과 건수만 갱신하도록 바꿨습니다. 한글 조합 중 입력 DOM이 유지되는 회귀 테스트도 추가했습니다.
- 이유: 화면 전체 재생성으로 검색 input이 교체되면서 `일본`이 `ㅇㅣㄹㅂㅗㄴ`처럼 분리되는 문제를 막기 위해서입니다.
- 검증: `일본` IME 조합 시 같은 input DOM과 조합 결과가 유지되고 일본 국가 카드가 검색되는지 Playwright로 확인했습니다. 전체 홈 E2E 테스트 72건이 통과했습니다.
- 남은 일: 없음
- 관련 파일: `indicators.html`, `scripts/test-home.mjs`, `WORKLOG.md`

### 2026-08-14 — README와 작업 로그의 역할 분리

- 작업자: Claude Code
- 변경: 같은 날 README에 넣었던 `작업 이력` 표와 `남은 일` 목록을 걷어내고 `WORKLOG.md`를 가리키게 했습니다. README에는 깨면 안 되는 제약(빌드 스텝 없음·클래식 스크립트 전역 노출·상대 경로·LF·`lib/valuation.js` 단일 출처), 설계 원칙, 파일 지도, 배치 테스트 사고 이력을 남겼습니다.
- 이유: 작업 로그가 도입되기 직전에 README에 이력을 넣어 두 곳이 겹쳤습니다. 이력이 두 파일에서 각자 자라면 어느 쪽이 최신인지 알 수 없게 됩니다.
- 검증: README·WORKLOG 양쪽에서 이력 항목이 한 번만 나오는 것과 상호 링크를 확인했습니다. 코드 변경이 없어 테스트는 돌리지 않았습니다.
- 남은 일: 아래는 README에서 옮겨온 미해결 항목입니다.
  - `docs/home-screen-spec.md`가 저장소에 추적되지 않고 로컬에만 있습니다. 추적할지 결정이 필요합니다.
  - 한국은행이 2027년 금통위 일정을 아직 공표하지 않았습니다(페이지가 빈 표를 반환). 공표되면 `data/macro-calendar.json`의 일정과 `coverage.BOK`를 갱신합니다.
  - KSIC 세분류(3~5자리) 한글명이 없습니다. 통계청 분류표를 받아 `scripts/lib/ksic.mjs`에 추가하면 됩니다.
  - `pages.yml`의 `workflow_run` 배포 경로를 아직 end-to-end로 관측하지 못했습니다. `push` 경로만 확인됐습니다.
  - `오늘의 주요 뉴스` 카드는 골격만 있습니다. 보유 자산과 연결된 뉴스만 담는다는 경계가 정해져 있습니다.
  - 경제지표 해설 문구와 ECOS·FRED 출처는 미구현입니다. 계열의 `source` 개념과 `<source>/<dataflow>.json` 파일 분리는 미리 해뒀습니다.
- 관련 파일: `README.md`, `WORKLOG.md`

### 2026-08-14 — 작업 공유 로그 도입

- 작업자: Codex
- 변경: README와 분리된 `WORKLOG.md`를 만들고 공통 기록 형식을 정했습니다.
- 이유: Codex와 Claude Code가 번갈아 작업해도 변경 내용과 검증 상태를 빠르게 이어받기 위해서입니다.
- 검증: 파일 생성과 Git 변경 상태를 확인했습니다.
- 남은 일: 이후 모든 기능·설정 변경에서 이 파일을 함께 갱신합니다.
- 관련 파일: `WORKLOG.md`

### 2026-08-13 — 공통 로그아웃 버튼 추가

- 작업자: Codex
- 변경: 공통 왼쪽 내비게이션 하단의 `내 자산` 옆에 로그아웃 아이콘 버튼을 추가했습니다.
- 이유: 어느 주요 화면에서든 설정 화면을 거치지 않고 로그아웃할 수 있게 하기 위해서입니다.
- 검증: 버튼 노출, 접근성 이름, 클릭 후 로그인 화면 이동, 배포 파일 반영을 확인했습니다.
- 남은 일: 없음
- 관련 파일: `lib/shell.js`, `lib/app.css`, `home.html`, `assets.html`, `settings.html`

### 2026-08-12 — 외부 사용자 가입용 이메일 발송 설정

- 작업자: Codex 및 사용자 설정
- 변경: Resend 발신 도메인 `auth.monasset.kr`을 인증하고 Supabase SMTP 연동을 구성했습니다.
- 이유: Supabase 기본 이메일 발송 제한에 막히지 않고 외부 사용자가 회원가입 확인 메일을 받을 수 있게 하기 위해서입니다.
- 검증: Resend 도메인 인증과 SMTP 구성 완료 화면을 확인했습니다.
- 남은 일: 새로운 외부 이메일로 회원가입부터 확인 메일, 로그인까지 종단간 재검증이 필요합니다.
- 관련 파일: Supabase·Resend 외부 설정

### 2026-08-11 — 로그인과 계정별 자산 저장 도입

- 작업자: Codex 및 Claude Code
- 변경: Supabase 이메일 인증, 계정별 세션 저장, 미로그인 접근 차단, 최소 설정 화면을 추가했습니다.
- 이유: 로컬과 배포 사이트 및 다른 기기에서 같은 계정의 자산 데이터를 공유하기 위해서입니다.
- 검증: 로그인 화면 우선 진입과 배포 설정 반영을 확인했습니다. RLS 정책 SQL과 검증 스크립트가 저장소에 있습니다.
- 남은 일: 계정 A와 B를 이용한 실제 RLS 격리 검증 결과를 완료 처리하기 전에 다시 확인해야 합니다.
- 관련 파일: `login.html`, `settings.html`, `lib/account-store.js`, `lib/supabase-config.js`, `supabase/schema.sql`

## 다음 작업자 주의사항

- `lib/valuation.js`와 `lib/portfolio.js`의 계산 로직은 저장 위치와 분리되어 있으므로 계정 저장 작업 때문에 바꾸지 않습니다.
- Supabase `service_role` 키와 SMTP 비밀번호는 브라우저 코드나 저장소에 넣지 않습니다.
- 조회 실패와 자산 0건 상태를 같은 화면으로 처리하지 않습니다.
- 네트워크 저장 실패 시 입력 내용은 로컬에 보존하고 사용자에게 실패를 알립니다.
- 스키마 버전이 달라도 사용자 데이터를 자동 삭제하지 않습니다.
- 현재 작업 트리에 자동 갱신 데이터인 `data/quotes.json`, `data/tickers-kr.json` 변경이 남아 있을 수 있습니다. 작업 범위가 아니면 임의로 포함하거나 되돌리지 않습니다.
