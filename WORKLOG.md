# 작업 공유 로그

Codex와 Claude Code가 번갈아 작업할 때 변경 맥락을 공유하는 파일입니다.
README는 설치·실행·구조 설명용으로 유지하고, 작업 이력과 인수인계 사항은 이 파일에 기록합니다.

## 기록 규칙

- 코드·설정·배포 관련 변경을 했다면 같은 커밋에서 이 파일도 갱신합니다.
- 최신 기록을 `최근 변경`의 맨 위에 추가합니다.
- 길게 설명하지 말고 변경 내용, 이유, 검증 결과, 남은 일을 적습니다.
- 실제로 확인하지 않은 항목은 완료로 쓰지 않습니다.
- 비밀 키, 비밀번호, SMTP 자격 증명, `service_role` 키는 기록하지 않습니다.
- 자동 생성 데이터만 바뀐 경우에는 사용자 기능에 영향이 있을 때만 기록합니다.

기록 형식:

```md
### YYYY-MM-DD — 작업 제목

- 작업자: Codex 또는 Claude Code
- 변경: 무엇을 바꿨는지
- 이유: 왜 바꿨는지
- 검증: 실제 확인한 내용
- 남은 일: 없으면 `없음`
- 관련 파일: `파일명`
```

## 현재 상태

- 서비스 주소: https://monasset.kr
- 저장소: https://github.com/oliversong25-lang/mon2
- 배포: GitHub Pages, `main` 브랜치
- 구성: 빌드 도구 없는 정적 HTML/CSS/JavaScript
- 인증·계정 저장: Supabase 이메일 로그인과 `user_asset_sessions` 테이블
- 데이터 보호: `auth.uid()` 기준 RLS 정책. 클라이언트에는 공개 가능한 publishable key만 사용
- 이메일 발송: Resend의 `auth.monasset.kr` 발신 도메인 인증 및 Supabase SMTP 연동
- 화면 흐름: 미로그인 사용자는 로그인 화면으로 이동하고, 로그인 후 자산 입력 또는 홈으로 진입
- 공통 내비게이션: 왼쪽 아래 `내 자산` 옆 로그아웃 버튼 제공

## 최근 변경

### 2026-08-18 — 미국 경기국면 단계 A-3 좌표층 재검증

- 작업자: Claude Code
- 변경: 좌표 표준화를 달력 창·최소 이력·완전 성숙으로 나눠 설정 가능하게 하고, 창 시작·끝·길이·관측 수·척도 대체를 감사 열로 남겼습니다. 좌표 창 후보 A(10년)·B(5년)·C(3년)·D(7년)·E(없음)와 진단에서 도출한 F·G(중심 0 고정, 척도만 추정)를 비교했습니다. 성숙도 시계를 원자료와 합성요인 둘로 나누고 상태를 `withheld`/`preliminary`/`official`로 정리해 사유를 기록합니다. Python 3.11 전용 검사 환경(`.venv311`)을 만들어 `mypy --strict`를 선언된 목표에서 통과시켰습니다.
- 이유: 단계 A-2의 10년 좌표 창은 지표 성숙 위에 좌표 성숙을 겹쳐 전체 요구를 15년으로 만들었고, 그래서 1990 시작 실행의 2001년 판정은 애초에 미성숙 판정이었습니다.
- 검증: 단계 A-3 **미통과**. 지정 후보 A~E 중 전 기준 통과 없음. B·C는 시작시점 차이를 3주·2주로 줄이지만 오탐률이 11.9%·17.5%로 뛰고 2022년 이후 오탐이 28주·60주가 됩니다. E는 표면 성능이 가장 좋지만 각도의 80%가 두 수직축에 몰려 원점 임계값 세 개가 의미를 잃습니다. 후보 G는 재현율 88.4%, 오탐률 3.01%, F1 78.1%, 2022년 이후 오탐 0주, 2001년 차이 8.0주로 필수 기준을 만족하지만 한계값에 여유가 없고 2020년 수축을 조기 판정합니다(구간 오탐 13주). 테스트 101개 통과, Ruff 통과, Python 3.11 mypy --strict 통과. 동결·ALFRED 모두 하지 않았습니다.
- 남은 일: 후보 G의 2020년 조기 판정 원인 분해, 2001년 8.0주의 잡음 민감도 확인, 점프 6건의 발생 시점 확인. ALFRED에는 `FRED_API_KEY`가 필요하며 현재 환경에 없습니다.
- 관련 파일: `model/src/business_cycle/models/momentum.py`, `model/src/business_cycle/validation/phase5.py`, `model/src/business_cycle/validation/phase5_report.py`, `model/configs/baselines.yaml`, `model/tests/test_phase5_coordinate_layer.py`, `model/outputs/robustness_validation/phase5/`

### 2026-08-17 — 미국 경기국면 단계 A-2 구성요소 분해와 corrected baseline

- 작업자: Claude Code
- 변경: `configs/baselines.yaml`로 legacy/corrected 설정을 파일에서 분리하고 `load_baseline()`을 추가했습니다. 10년 causal rolling 표준화에 창 시작·끝·관측 수 감사와 문서화한 척도 대체(표준편차→IQR→expanding)를 붙였고, 좌표(X·Y) 표준화에 방식·창·최소 이력 설정을 새로 열었습니다. 상태필터에 감사용 초기분포 인자를 추가하고, 영역별 상한 재정의와 실업수당 동일가중 부요인을 구현했습니다. `validation/phase4.py`가 2x2x2 ablation, 날짜별 워밍업 분해, 시작시점 수렴, 상태 초기값 감사, 실업수당 3안 비교, LOO를 한 번에 실행합니다.
- 이유: phase3는 robust6 한 줄만 보고 단계 A를 판정했습니다. 요인을 분리해 보니 robust6 단독은 재현율을 오히려 올리고, 악화는 빈도 수정과의 상호작용에서 나옵니다. 또 워밍업 의존성의 원인은 지표 전처리가 아니라 최소 이력 규칙이 없던 좌표 표준화였습니다.
- 검증: 단계 A-2 **미통과**. corrected baseline 재현율 84.3%(기준 85%), 오탐률 2.68%, 점프 4, 왕복 13, 2022년 이후 오탐 0주. 1985·1990의 2001 진입 차이는 좌표 최소 이력을 넣어 44주→21주, 완전성숙 후 국면 불일치 11.3%→2.4%로 줄었지만 8주 기준은 만족하지 못했습니다. 상태 초기값 영향은 104주에 1.4e-6까지 감쇠해 2001년 차이의 원인이 아닙니다. LOO에서 어떤 지표를 빼도 현재 대국면이 유지됩니다. 테스트 76개 통과, Ruff 통과, mypy 통과. 설정 동결과 ALFRED는 하지 않았습니다.
- 남은 일: 재현율 85% 미달과 금융위기 진입 지연의 원인이 3년 추세 길이인지 확인하는 것. 2001 진입 차이 기준은 미성숙 판정을 비교하는 문제라 기준 자체를 다시 정의해야 합니다.
- 관련 파일: `model/configs/baselines.yaml`, `model/src/business_cycle/validation/phase4.py`, `model/src/business_cycle/models/momentum.py`, `model/src/business_cycle/preprocessing/standardize.py`, `model/tests/test_phase4_corrected_baseline.py`, `model/outputs/robustness_validation/phase4/`

### 2026-08-17 — 미국 경기국면 단계 A 빈도·워밍업·극단값 재검증

- 작업자: Codex
- 변경: 원빈도 기준 달력 3년 추세(주간 156·월간 36·분기 12), 현재값을 제외한 rolling median/MAD와 causal IQR·표준편차 fallback, 합성 기여 직전 Huber 제한, 5~10년 지표 성숙도 ramp를 구현했습니다. 기존→달력3년→성숙도→robust6, 표준화 6종, 추세 2·3·5년, 1985·1990 워밍업, 7개 지표 LOO를 실자료로 다시 계산했습니다.
- 이유: 기존 `156`이 주간에는 156주, 월간에는 156개월로 적용됐고 팬데믹 극단값과 짧은 워밍업에 결과가 취약했습니다. 원신호·제한 전·제한 후 값을 모두 남겨 충격을 지우지 않고 영향력만 제한하도록 했습니다.
- 검증: 변경 전 기준값(재현율 93.4%, 오탐률 6.34%, 점프 4, 왕복 20)을 정확히 재현했습니다. robust6은 팬데믹 최초/4주 확인 지연 2주/5주와 5개 경제영역 악화를 유지하고 오탐률을 4.25%로 낮췄지만, 재현율 81.8%, 점프 7, 왕복 30으로 채택 기준을 통과하지 못했습니다. 1985·1990의 2001 진입 차이는 44주에서 25주로 줄었습니다. 단계 A 미통과로 설정을 동결하지 않았고 ALFRED도 시작하지 않았으며, 기본 `model.yaml`은 기존 후보 설정을 유지합니다.
- 남은 일: median/MAD 후보의 재현율과 국면 안정성 저하 원인을 구조적으로 분석해야 합니다. 수치 맞춤 탐색 없이 표준화 창·좌표 단계와 Huber 적용 범위의 경제적 근거를 먼저 검토합니다. ALFRED는 단계 A 통과 뒤에만 진행합니다.
- 관련 파일: `model/src/business_cycle/preprocessing/standardize.py`, `model/src/business_cycle/preprocessing/transforms.py`, `model/src/business_cycle/models/composite.py`, `model/src/business_cycle/validation/phase3.py`, `model/tests/test_phase3_robustness.py`, `model/outputs/robustness_validation/phase3/`

### 2026-08-17 — 미국 경기국면 모델 2.5차 강건성 검증 및 ALFRED 중단

- 작업자: Codex
- 변경: 전 주차 유효 가중치·2022년 기여도, 7개 지표 leave-one-out, 5개 파라미터 ±10/20% 단독 민감도, 1980/1985/1990 워밍업과 1995/2000 검증 시작, 잔여 점프·왕복·고확실성 오탐, 확실성 구간을 감사했습니다. 결측 재정규화 fixture에서 동일 영역 상한이 33.62%까지 올라가는 버그를 발견해 영역별 잔여용량을 한 번만 배분하도록 수정했습니다.
- 이유: ALFRED 결과를 보기 전에 채택 설정이 특정 지표·파라미터·시작 시점에 과도하게 의존하지 않고 가중치 제약을 항상 지키는지 확인해야 했습니다.
- 검증: 공식 검증 주차의 개별 20%·영역 30% 위반은 0건이며, 수정 후 결측 fixture도 두 상한을 통과합니다. 2차 동일 조건을 재실행해 재현율 93.4%, 오탐률 6.34%, 정밀도 53.8%, F1 68.3%, 점프 4건, 왕복 20건, 2022년 이후 오탐 5주가 유지됨을 확인했습니다. 그러나 1990 워밍업에서 오탐률 13.02%·정밀도 35.8%, 2001 진입 40주 선행으로 악화됐습니다. pytest 35건, Ruff format/lint, strict mypy가 모두 통과했습니다.
- 남은 일: `trend_span_weeks=156`이 월간에는 156개월, 주간에는 156주로 적용되는 빈도 단위 불일치와 명시적 이상치 완화 부재를 해결해야 합니다. 단계 A는 미통과이며 설정·해시를 동결하지 않았고 ALFRED 수집과 빈티지 백테스트는 시작하지 않았습니다. 구조 수정 후 2차 baseline과 강건성 검증을 다시 통과해야 ALFRED를 재개할 수 있습니다.
- 관련 파일: `model/src/business_cycle/models/composite.py`, `model/src/business_cycle/validation/robustness.py`, `model/outputs/robustness_validation/`, `model/tests/test_robustness.py`, `model/configs/model.yaml`

### 2026-08-16 — 미국 경기국면 모델 v0.1 2차 FRED 실자료 보정

- 작업자: Codex
- 변경: 침체 양성을 `contraction` 3개 세부국면으로만 고정하고 평가 산식·USREC 주간 정렬·4주 causal 확인일을 감사했습니다. 기존 8주 모델의 오탐 350주와 2022년 이후 77주를 에피소드·지표 기여도로 분해한 뒤, 약한 Y 수준에서 침체 관측확률을 연속적으로 낮추는 게이트와 원점 부근에서만 인접 이동을 강제하는 최소 규칙을 추가했습니다. 4주·8주·후보 2개·Dynamic 비교표, 12개 차트, JSON·CSV·Markdown 보고서를 생성했습니다.
- 이유: 기존 모델은 원점 부근의 약한 음수 X·Y에서도 각도만으로 침체를 확정해 정상기 오탐이 길게 이어졌고, Y 게이트만 적용하면 다단계 점프가 22건으로 늘어나는 부작용이 있었습니다.
- 검증: 최종 후보는 전체기간 재현율 93.4%, 오탐률 6.34%, 정밀도 53.8%, F1 68.3%, 다단계 점프 4건입니다. 기존 8주 모델의 94.2% 재현율과 비교해 손실은 0.8%p이며 오탐률은 22.9%에서 6.34%로 감소했습니다. 2020 침체 최초 판정은 두 모델 모두 2주 지연, 4주 확인은 5주 지연으로 유지됐습니다. pytest 32건, Ruff format/lint, strict mypy가 모두 통과했습니다.
- 남은 일: 최신 수정치 기반 preliminary 검증이므로 당시 공개정보 ALFRED vintage 검증이 필요합니다. 2021년 이후 구간은 이미 본 observed diagnostic holdout이며, 설정을 동결한 뒤 새 발표분으로 prospective 검증해야 합니다. 13주 전망은 계속 `not_calibrated`입니다.
- 관련 파일: `model/src/business_cycle/models/`, `model/src/business_cycle/validation/phase2*.py`, `model/configs/model.yaml`, `model/outputs/real_data_validation/phase2/`, `model/tests/test_phase2_validation.py`

### 2026-08-16 — 미국 경기국면 모델 v0.1 공식 FRED 실자료 검증

- 작업자: Codex
- 변경: FRED 수집기의 간헐 timeout 원인을 User-Agent별 응답 차이로 특정하고, 제한 재시도·부분 성공 캐시·신선 캐시 재사용·지표별 출처/수집시각/관측기간/실패 기록을 추가했습니다. 공식 대표 좌표를 `CompositeFactorModel`로 바로잡고 `DynamicFactorModel`은 비교용으로 분리했습니다. 1985년부터 워밍업 자료를 수집해 1995-01-06~2026-08-14 baseline과 8주 모멘텀 최소 조정안을 검증했으며, 공식 FRED `USREC` 비교·역사 사례·모든 점프/왕복 진단·10개 차트를 생성했습니다.
- 이유: 기존 30초 단일 요청은 첫 지표 지연 시 전체를 잃었고, 파이프라인은 명세와 달리 동적요인을 대표 좌표에 사용했습니다. 또한 1995년부터만 수집하면 추세·표준화 워밍업 때문에 1999년 이전 판정이 사라졌습니다.
- 검증: 공식 FRED 핵심 7개 지표와 `USREC`를 수집했습니다. 조정안은 세부국면 변경 424→332회, 다단계 점프 21→7회, 왕복 40→37회, NBER 침체 재현율 88.4%→94.2%로 개선됐지만 오탐률 22.9%·정밀도 24.6%여서 운영 판정용으로는 부족하다고 보고서에 명시했습니다. pytest 24건, Ruff format/lint, strict mypy가 모두 통과했습니다.
- 남은 일: NBER 비침체 오탐의 구조적 원인 분해, ALFRED 실시간 빈티지·실제 발표일 연결, 12개 세부국면의 별도 경제적 검증 설계가 필요합니다.
- 관련 파일: `model/src/business_cycle/data/fred.py`, `model/src/business_cycle/validation/`, `model/configs/model.yaml`, `model/outputs/real_data_validation/`, `model/tests/`

### 2026-08-16 — 독립 미국 경기국면 모델 v0.1

- 작업자: Codex
- 변경: 기존 서비스와 분리된 `model/` Python 패키지에 FRED·로컬 수집, causal 전처리, 합성지수와 결측 허용 Kalman 동적요인, 12국면 확률·순환 전이, 세 확실성, JSON·CSV·Markdown 출력, walk-forward 백테스트와 NBER 침체 비교를 구현했습니다.
- 이유: 프런트엔드에 바로 결합하기 전에 미래정보 누출을 통제하며 반복 백테스트할 수 있는 독립 연구 기반을 만들기 위해서입니다.
- 검증: 합성 데이터 오프라인 데모로 보고서 4종을 생성했습니다. pytest 19건, Ruff format·lint, strict mypy가 모두 통과했습니다.
- 남은 일: 실제 FRED 공통기간 preliminary 백테스트, ALFRED 빈티지 연결, 발표일 정교화, 각도·전이·확실성·13주 확률 calibration이 필요합니다.
- 관련 파일: `model/`, `WORKLOG.md`

### 2026-08-15 — 경제지표 탭이 두 인덱스를 합쳐 읽음

- 작업자: Claude Code
- 변경: 화면이 `index.json`(OECD 월·분기)과 `index-daily.json`(일간 금리)을 합쳐 읽습니다. 검색은 합친 목록을 훑으므로 두 출처가 모두 걸립니다. **한쪽이 비면 어느 쪽이 비었는지 배너로 밝힙니다.** 주기 표기에 `일간`이 생겼습니다.
- 이유: 인덱스를 나눈 것은 배치가 서로의 산출물을 덮어쓰지 않게 하려는 것인데, 화면이 한쪽만 읽으면 사용자는 나머지가 원래 없는 줄 압니다 — 없는 것과 못 받은 것을 뭉치는 셈입니다.
- 검증: 전체 회귀 테스트 283건 통과. 합치면서 두 가지가 깨졌고 고쳤습니다 — (1) `headlineSeries`를 빠뜨려 홈 지표 카드가 통째로 비었고, (2) `periodToMonths`가 일간 날짜(`2026-08-14`)를 못 읽어 전 계열을 "오래됨"으로 판정했습니다.
- 남은 일: 다음 갱신 예정일 줄(Change B)은 시작하지 못했습니다 — 아래 참고.
- 관련 파일: `lib/indicators-view.js`, `indicators.html`, `scripts/test-home.mjs`

**공공데이터포털 키가 바뀌었습니다.** 특일정보가 계속 401 `SERVICE_KEY_IS_NULL`이라 인코딩 문제인지 확인하려고 **같은 키로 주식시세정보를 쳐 봤더니 그쪽도 401**이었습니다. 몇 시간 전 같은 키·같은 인코딩으로 정상 동작하던 엔드포인트입니다. 전파 지연이나 인코딩이 아니라 키 자체가 재발급·폐기된 것입니다. 새 키가 있어야 휴장일 달력을 붙일 수 있습니다.

### 2026-08-15 — 일간 금리 수집 (ECOS · 미 재무부 · 뉴욕 연준)

- 작업자: Claude Code
- 변경: 매일 바뀌는 금리 26계열을 받는 배치(`build-daily-rates.mjs`)와 전용 워크플로(`daily-rates.yml`)를 추가했습니다. OECD 배치와 분리했습니다 — 원천 주기가 다르고 한쪽이 죽어도 다른 쪽은 갱신돼야 합니다. FRED는 쓰지 않습니다.
- 이유: 앱에 매일 바뀌는 경제지표가 하나도 없었습니다. OECD 계열은 원천이 월·분기라 매일 돌 이유가 없고, 국채 금리와 정책금리는 매일 바뀌는데 아예 없었습니다.
- 검증: ECOS 키 확인 완료(아래). 배치 실측 4.4초, 실패 0건. 연속 실행으로 `updated` → `already-current` 두 결과를 확인했습니다. 전체 회귀 테스트 283건 통과.
- 남은 일: 경제지표 탭이 아직 `index-daily.json`을 읽지 않습니다(현재 `index.json`만 읽습니다). 다음 갱신 예정일 줄(Change B)도 남았습니다 — 특일정보 API가 아직 401입니다.
- 관련 파일: `scripts/lib/daily-rates.mjs`, `scripts/build-daily-rates.mjs`, `.github/workflows/daily-rates.yml`, `.github/workflows/pages.yml`

**ECOS 키 검증**

`KeyStatisticList` HTTP 200, 101개 통계 정상 반환. 오류 코드 없음. 앞서 빈 응답이 나온 것은 키 대기가 아니라 **시크릿 이름 불일치**였을 가능성이 큽니다 — 저장소는 `ECOS_AUTH_KEY`를 쓰는데 `ECOS_API_KEY`로 등록돼 있으면 `undefined`가 넘어가고 ECOS는 그걸 빈 몸통으로 돌려줍니다. 배치는 두 이름을 모두 읽습니다.

**일간(D) 주기 계열**: ECOS 통계표 834개 중 일간은 7개뿐이고 그중 금리는 `722Y001`(기준금리)·`817Y002`(시장금리 일별) 둘입니다. OECD 계열은 원천이 월·분기라 일간이 구조적으로 불가능합니다.

**출처 선택**

- 미 국채: 재무부 XML 피드 직접. 키 없음, 미국 정부 저작물. FRED는 이걸 재배포할 뿐이라 라이선스 판단을 하나 없앴습니다.
- 연준 정책금리: **뉴욕 연준** `markets.newyorkfed.org/api/rates/all/latest.json`. EFFR을 산출·공표하는 주체이고, 같은 행에 FOMC 목표범위(`targetRateFrom`/`targetRateTo`)가 함께 옵니다.

### 2026-08-15 — 외화·가상자산의 전일 값과 변화 문구 정리

- 작업자: Claude Code
- 변경: 환율은 `searchdate`로 전일 값을 직접 받아 `prevRates`·`prevRatesDate`로 싣고, 가상자산은 코인게코의 24시간 변동률로 24시간 전 가격을 되돌려 `prev`로 싣습니다. 총자산 카드는 기준일을 레일에 앞세우고 변화는 날짜 없이 "전일 대비"로만 적습니다 — 기준선이 직전 거래일이 **아닐 때만** 날짜를 붙입니다.
- 이유: USD 현금·비트코인·이더리움 세 건이 전일 값이 없어 변화 계산에서 빠져 있었습니다. 그리고 카드에 날짜가 "8월 12일 대비" 하나뿐이라 "이 데이터는 8월 12일 것"으로 읽혔고, 그 오해가 파이프라인이 멈췄다는 신고로 여러 번 돌아왔습니다.
- 검증: 전체 회귀 테스트 283건 통과. 코인게코 필드는 살아 있는 응답으로 확인했습니다(아래). 비영업일 환율 조회가 "어제"를 가정하지 않고 데이터가 있는 날까지 거슬러 올라가는지, 정상일 때 변화 문구에 날짜가 없고 평일이 낀 경우에만 날짜가 붙는지를 테스트로 고정했습니다.
- 남은 일: 은(ECOS)은 여전히 전일 값이 없어 제외됩니다.
- 관련 파일: `scripts/build-quotes.mjs`, `scripts/lib/crypto.mjs`, `lib/valuation.js`, `home.html`, `lib/app.css`, `scripts/test-quotes-mock.mjs`, `scripts/test-home.mjs`

**코인게코 필드 (2026-08-15 실제 응답)**

`include_24hr_change=true`를 주면 `krw_24h_change`가, `include_last_updated_at=true`를 주면 `last_updated_at`(초)이 붙습니다.

```json
{"bitcoin":{"krw":89263800,"krw_24h_change":-0.4293991463197649,"last_updated_at":1786774430}}
```

24시간 전 가격은 `price / (1 + change/100)`으로 되돌립니다. **이것은 거래소 전일 종가가 아니라 24시간 롤링 값입니다** — 기준 시점이 국내 종가와 달라서 `cryptoPrevBasis: "24h"`로 산출물에 남기고, 화면도 가상자산이 포함되면 "가상자산은 24시간 전 대비"라고 한 줄 적습니다.

**비영업일 환율 조회**

수출입은행은 비영업일에 데이터를 주지 않고 `result:3` 한 건만 돌려줍니다. 기존 `fetchFxRates`가 이미 데이터가 있는 날까지 최대 10일 거슬러 올라가므로 전일 환율도 같은 함수를 하루 전부터 돌립니다 — **"어제"라고 가정하지 않습니다.** mock에서 사이 이틀을 비워 두고 사흘 전 날짜로 내려앉는지 검사합니다.

### 2026-08-15 — 총자산 변화의 기준선을 시세 데이터로 옮김

- 작업자: Claude Code
- 변경: 배치가 종목마다 전일 종가(`prevClose`)와 그 날짜(`prevCloseDate`)를 함께 싣고, 홈 화면은 **지금 들고 있는 자산을 전일 종가로 다시 평가**해 변화를 냅니다. 전일 값이 없는 자산은 빼고 건수를 밝힙니다. 응답에 전일 종가 필드가 없어 `clpr - vs`로 만들되, `vs`가 정말 전일 대비인지를 같은 행의 등락률로 **매 실행 검증**합니다(`vs / (clpr - vs) * 100 ≈ fltRt`).
- 이유: 예전 기준선은 이 브라우저가 마지막으로 본 스냅샷이었습니다. 며칠 만에 열면 며칠치 변화가 나오고(배포가 막혀 asOf가 08-07에 멈춰 있던 엿새 뒤 08-12로 뛰자 "8월 7일 대비"가 찍혔습니다), 스냅샷이 총액을 담고 있어 **자산을 추가하면 그 금액이 통째로 변동으로 잡혔습니다**(5,000만원 예금을 넣으면 5,000만원 오른 것처럼 보임).
- 검증: 전체 회귀 테스트 281건 통과. 2026-08-13 실제 응답으로 네 출처 모두 `clpr`·`vs`·`fltRt`를 주는 것과 항등식이 성립하는 것을 확인했습니다(최대 오차 주식 0.00495 · ETF 0.00500 · ETN 0.00500 · 금 0.00059 %p). 5,000만원 예금을 추가해도 delta가 변하지 않는 것, 전일 종가가 없는 자산이 0이 아니라 제외로 처리되고 건수가 화면에 뜨는 것, 일부러 망가뜨린 행이 배치를 멈추고 원본 행을 통째로 찍는 것을 테스트로 고정했습니다.
- 남은 일: 가상자산과 외화는 전일 값이 없어 변화 계산에서 빠집니다(코인게코는 전일 종가를, 수출입은행 환율은 전일 환율을 싣지 않습니다). 필요하면 배치가 직전 산출물의 값을 보존해 채울 수 있습니다.
- 관련 파일: `scripts/build-quotes.mjs`, `lib/valuation.js`, `home.html`, `scripts/test-quotes-mock.mjs`, `scripts/test-home.mjs`

**출처별 전일 종가 가용성 (2026-08-13 실제 응답)**

| 출처 | 행 수 | `clpr` | `vs` | `fltRt` | 전일 종가 |
|---|---:|---|---|---|---|
| 주식시세정보 | 2,872 | 있음 | 있음 | 있음 | **만들 수 있음** |
| 증권상품시세정보(ETF) | 1,163 | 있음 | 있음 | 있음 | **만들 수 있음** |
| 증권상품시세정보(ETN) | 370 | 있음 | 있음 | 있음 | **만들 수 있음** |
| 일반상품시세정보(금) | 2 | 있음 | 있음 | 있음 | **만들 수 있음** |
| 한국은행 ECOS(은) | – | – | – | – | 없음 — 제외 |
| CoinGecko(가상자산) | – | – | – | – | 없음 — 제외 |
| 한국수출입은행(환율) | – | – | – | – | 없음 — 외화 자산 제외 |

전일 종가 필드는 어느 출처에도 없습니다. 넷 다 `clpr - vs`로 만들고, 넷 다 같은 항등식으로 따로 검증합니다.

`data/quotes.json` 크기: **179.3KB → 254.6KB** (+75.3KB, +42%, 종목당 약 17.5바이트).

### 2026-08-15 — 배치 산출물 캐시 무효화와 예약 슬롯 이동

- 작업자: Claude Code
- 변경: 앱이 받는 데이터 파일을 `lib/fetch-data.js` 한 곳으로 모으고 파일별 캐시 정책을 붙였습니다. 매일 바뀌는 파일(`quotes.json`, `indicators/index.json`, `indicators/latest/*`, `indicators/oecd/*`, `macro-calendar.json`)은 `cache: "no-cache"`로 매번 서버에 확인하고, 주 1회 바뀌는 종목 목록(`tickers-*.json`, 합계 4MB)은 기본 캐시를 그대로 씁니다. `quotes.yml`의 cron을 `0 5`에서 `17 5`로 옮기고 `43 9`(18:43 KST) 두 번째 슬롯을 더했습니다.
- 이유: 배치는 커밋했고 배포도 됐는데 브라우저가 어제 파일을 들고 있었습니다(실측: 그냥 요청하면 asOf 2026-08-12, 캐시를 비껴가면 2026-08-13). 화면에는 배치가 고장 난 것과 똑같이 보여서 파이프라인 쪽을 며칠 뒤졌습니다. 코드 파일은 이미 `?v=` 표식을 달고 있었는데 정작 매일 바뀌는 데이터에는 아무것도 없었습니다. 예약 슬롯은 정각이 가장 붐벼 75분 밀린 실행이 있었습니다.
- 검증: 전체 회귀 테스트 269건 통과. 신규 3건이 이걸 못 박습니다 — 파일을 다시 쓴 뒤 **손으로 캐시를 비껴가지 않고** 연속 두 번 로드해 둘 다 새 asOf를 보는지, 파일별 캐시 모드가 의도대로인지(`quotes`·`macro`·`indicators` = no-cache, `tickers` = 기본), 캐시에서 나온 낡은 값을 배치 문제와 구별해 알리는지.
- 남은 일: 총자산 변화의 기준선을 시세 데이터에서 뽑는 작업(전일 종가 기반)은 시작하지 못했습니다. 금융위원회 API의 전일 대비 필드명을 살아 있는 응답으로 확인해야 하는데 로컬에 `DATA_GO_KR_KEY`가 없습니다.
- 관련 파일: `lib/fetch-data.js`, `lib/valuation.js`, `lib/indicators-view.js`, `home.html`, `assets.html`, `asset-input.html`, `indicators.html`, `.github/workflows/quotes.yml`, `scripts/test-home.mjs`, `scripts/test-focus.mjs`

**앱이 받는 데이터 파일과 캐시 결정**

| 파일 | 배치 갱신 주기 | 결정 |
|---|---|---|
| `data/quotes.json` | 매일 | 조건부 요청(no-cache) |
| `data/indicators/index.json` | 매일 | 조건부 요청 |
| `data/indicators/latest/*.json` (7개) | 매일 | 조건부 요청 |
| `data/indicators/oecd/*.json` (13개) | 매일 | 조건부 요청 |
| `data/macro-calendar.json` | 손으로 유지 | 조건부 요청 (48KB, 갱신하면 바로 보여야 함) |
| `data/tickers-{kr,us,global}.json` | 주 1회 | **기본 캐시** (합계 4MB) |

`?t=Date.now()`를 쓰지 않은 이유: 캐시는 확실히 비껴가지만 매 로드가 전량 재다운로드가 되고 CDN 입장에서는 매번 다른 URL이라 캐시가 아예 서지 않습니다. `?v=` 버전 문자열도 안 됩니다 — 빌드 스텝이 없고 데이터는 코드 변경 없이 바뀌므로 배치가 HTML을 고치지 않는 한 표식이 따라가지 못합니다. `cache: "no-cache"`는 캐시를 끄는 게 아니라 매번 서버에 물어보게 하는 것이라, 내용이 그대로면 304로 본문 없이 끝납니다.

### 2026-08-14 — OECD 지표 7개 → 50개 확장과 경제지표 탭 개편

- 작업자: Claude Code
- 변경: OECD 수집을 7지표 250계열에서 **50지표 2,064계열 55개국**으로 넓혔습니다. 7개 갈래(물가·고용·경기·생산·금리·금융·무역·성장)로 나누고, 카탈로그를 `index.json`(26KB) + `latest/<갈래>.json` + `oecd/<dataflow>.json` 3단으로 쪼갰습니다. 경제지표 탭을 갈래 1차 축으로 개편했고(갈래 → 지표 → 국가), 국가 필터와 국가 우선 피벗을 함께 뒀습니다. 금리·주가·환율(`DF_FINMARK`)이 이번에 처음 들어왔습니다.
- 이유: 지표 7개는 제 판단으로 좁혀 둔 것이었지 OECD의 한계가 아니었습니다. 다만 공개 엔드포인트의 dataflow 1,546개를 전 계열로 담으면 150MB가 넘어 매일 그만큼을 커밋하게 되므로, 계열을 통째로 받지 않고 헤드라인만 selector로 집는 쪽을 골랐습니다(사용자 승인). 지표가 50개가 되면 국가 1차 축으로는 "물가가 어디 있지"를 찾을 수 없어 갈래가 1차 축이어야 했습니다.
- 검증: 전체 회귀 테스트 266건 통과(지표 mock 24건, 홈 83건). 배치 완주 실측 105~120초, 실패 0건. 산출물은 `index.json` 26KB · 갈래 파일 7개(22~40KB) · 시계열 4.2MB. 3.9 항목을 테스트로 고정했습니다 — 기본 화면에 안 열린 지표(실질실효환율)가 검색으로 찾히는지, 갈래 우선과 국가 우선이 같은 값에 닿는지, 한 flow가 죽어도 나머지가 갱신되고 실패가 엔드포인트 이름과 함께 남는지.
- 남은 일: ECOS는 다음 차례입니다. 계열 id가 `<출처>:<지표>:<국가>`이고 지표 정의에 `source`가 따로 있어, `scripts/lib/indicators.mjs`에 source가 다른 배열을 이어 붙이면 기존 파일 모양을 바꾸지 않고 들어옵니다. 해설·활용방안 문구는 아직 범위 밖입니다.
- 관련 파일: `scripts/lib/indicators.mjs`, `scripts/lib/oecd.mjs`, `scripts/build-indicators.mjs`, `lib/indicators-view.js`, `indicators.html`, `home.html`, `data/indicators/**`, `scripts/test-indicators-mock.mjs`, `scripts/test-home.mjs`

**이번에 부딪힌 것 세 가지 (다음 사람이 같은 데서 멈추지 않도록)**

1. **OECD는 분당 호출 한도가 있습니다.** dataflow 호출이 4번에서 13번이 되자 다섯 번째부터 전부 429였고(13개 중 8개 실패), 계속 두드리자 IP가 더 오래 막혀 13개 전부 실패했습니다. `scripts/lib/oecd.mjs`가 호출 간격을 8초로 벌립니다. 회귀 테스트는 `OECD_MIN_GAP_MS=0`으로 끕니다 — 안 끄면 한 번 도는 데 5분을 넘깁니다.
2. **dataflow 버전을 전부 `1.0`으로 두면 STES 계열이 통째로 404납니다**(`DF_CLI` 4.1, `DF_INDSERV` 4.3).
3. **선택자는 반드시 실제 코드리스트를 보고 골라야 합니다.** 추측하면 예외가 아니라 관측 0건으로 조용히 사라집니다. 첫 실행에서 세 지표가 비었고, 수주잔량(`OB`)은 한국이 제출하지 않아 수주 유입(`OI`)으로, 상품·서비스 수지는 GDP 대비가 아니라 달러 금액으로만 발행돼 `USD_EXC`로 고쳤습니다.

### 2026-08-14 — 화면 디자인 개편 (전 화면)

- 작업자: Claude Code
- 변경: 강조색을 없애고 조작 가능한 것을 전부 무채색 본지(`--action #f0ede4`)로 바꿨습니다. 배경을 근-검정에서 잉크 그라파이트로 올리고, 활자를 세 역할(Hahmlet 명조 표제 · Pretendard 본문 · IBM Plex Mono 유틸리티)로 나눴습니다. 홈에는 총자산의 억·만·원 단위 조판과 기준일 레일을 넣었습니다. `asset-input.html`이 통째로 복사해 갖고 있던 토큰을 걷어내고 `lib/app.css`를 쓰게 했습니다.
- 이유: 예전 `--brand #5b7cfa`가 하락색 `--neg #5b8dee`와 ΔE 6.1로 사실상 같은 색이라, 버튼·포커스·활성 내비가 "하락"과 구별되지 않았습니다. 색을 상승·하락과 구성에만 남기면 이 화면에서 색이 한 가지만 뜻하게 됩니다.
- 검증: 전체 회귀 테스트 254건 통과. 도넛 6색은 바꾸지 않았고 새 표면(`#1a1c26`)에서 대비 최저 4.30:1로 3:1 기준을 유지합니다(기존 표면 4.36:1). 상단 3열 비율은 1.35:1:1을 실측 확인했고, 폰트 세 종이 모든 화면에서 실제 적용되는 것을 확인했습니다. 1440/1024/768/390px에서 가로 넘침 0px, `prefers-reduced-motion`에서 레일 애니메이션이 꺼지는 것, 포커스 링(2px solid)이 보이는 것도 확인했습니다.
- 남은 일: 도넛 팔레트의 커밋된 CVD 수치(인접쌍 최저 ΔE 8.4)가 재현되지 않습니다. CIEDE2000 + Viénot로 다시 재면 최저값이 1.6이고(보라 `#9085e9` ↔ 파랑 `#3987e5`, 적색맹, 도넛 12시에서 맞닿는 쌍), 3색맹에서는 `#c98500` ↔ `#d55181`이 2.1입니다. 원래 검증이 다른 모델·다른 공식이었을 수 있어 팔레트는 건드리지 않았습니다. 색을 바꾸려면 먼저 어느 방법으로 8.4가 나왔는지 확인해야 합니다.
- 관련 파일: `lib/app.css`, `home.html`, `assets.html`, `asset-input.html`, `indicators.html`, `login.html`, `settings.html`, `index.html`, `scripts/test-home.mjs`, `scripts/lib/test-auth.mjs`

### 2026-08-14 — 거시 일정 확대(금리 결정 → 주요 지표)와 주별·월별 보기

- 작업자: Claude Code
- 변경: `다가오는 일정`의 범위를 금리 결정에서 한국·미국 주요 지표 발표까지 넓혔습니다(24건 → 74건). 일정마다 `category`·`country`·`importance`·`dateBasis`를 붙였고, 컴팩트 카드와 확대 보기 모두에 `가까운 순 · 주별 · 월별` 전환을, 확대 보기에는 국가·중요도 필터를 넣었습니다. 파일은 `scripts/build-macro-calendar.mjs`가 생성합니다 — 날짜는 스크립트 안에 손으로 적혀 있고 외부 API는 부르지 않습니다.
- 이유: 금리 결정만 담은 것은 단계적 결정이었지 목표 상태가 아니었습니다. 일정이 74건이 되면 가까운 순 한 줄 목록으로는 "이번 주에 뭐가 있나"를 볼 수 없어 주·월 묶음이 함께 필요했습니다. 생성기를 둔 것은 이 파일에서 틀리기 쉬운 것이 날짜가 아니라 시간대 환산이기 때문입니다 — 08:30 ET는 한국에서 같은 날, 14:00 ET는 다음 날이고 서머타임 경계에서 또 갈립니다.
- 검증: 전체 회귀 테스트 254건 통과(홈 81건, 신규 9건 포함 · Codex의 IME 수정 위에 재적용). 시간대는 실측으로 확인했습니다 — 08:30 ET는 서머타임 21:30·이후 22:30으로 **같은 날**, 14:00 ET는 03:00·04:00으로 **다음 날**. 서머타임 경계(2026-11-01) 양쪽에서 값이 실제로 갈리는지도 테스트가 봅니다. 주 경계는 8/31(월)과 9/6(일)이 같은 주로 묶이고 9/7이 다음 주로 갈리는 것을 확인했습니다. 상단 3열 행 높이는 변경 전후 모두 327px로 같고(일정 카드 자연 높이 207 → 236, 행보다 작음), 로컬 서버에서 실제 클릭으로 주별 전환·확대 보기·출처 링크를 확인했습니다.
- 남은 일: ISM 공표 달력(ismworld.org)이 로그인을 요구해 열지 못했습니다. ISM 8건과 산업통상부 수출입 동향 4건은 기관이 밝힌 규칙(첫/셋째 영업일, 매월 1일)으로 날짜를 잡고 `dateBasis: "rule"`로 표시했으며 화면에도 `예정` 표시가 붙습니다. 공휴일에 밀릴 수 있으니 달력을 열 수 있게 되면 확정 날짜로 교체해야 합니다. 미 노동통계국(bls.gov)은 자동 조회를 차단(403)해 BLS 일정을 그대로 싣는 페이지에서 확인했고, 갱신할 때는 브라우저로 직접 열어야 합니다. 2027년치는 대부분의 기관이 아직 공표하지 않아 FOMC만 들어 있습니다.
- 관련 파일: `scripts/build-macro-calendar.mjs`, `data/macro-calendar.json`, `home.html`, `scripts/test-home.mjs`, `README.md`

### 2026-08-14 — 경제지표 검색 한글 IME 오류 수정

- 작업자: Codex
- 변경: 경제지표 검색 시 화면 전체를 다시 그리지 않고 결과 목록과 건수만 갱신하도록 바꿨습니다. 한글 조합 중 입력 DOM이 유지되는 회귀 테스트도 추가했습니다.
- 이유: 화면 전체 재생성으로 검색 input이 교체되면서 `일본`이 `ㅇㅣㄹㅂㅗㄴ`처럼 분리되는 문제를 막기 위해서입니다.
- 검증: `일본` IME 조합 시 같은 input DOM과 조합 결과가 유지되고 일본 국가 카드가 검색되는지 Playwright로 확인했습니다. 전체 홈 E2E 테스트 72건이 통과했습니다.
- 남은 일: 없음
- 관련 파일: `indicators.html`, `scripts/test-home.mjs`, `WORKLOG.md`

### 2026-08-14 — README와 작업 로그의 역할 분리

- 작업자: Claude Code
- 변경: 같은 날 README에 넣었던 `작업 이력` 표와 `남은 일` 목록을 걷어내고 `WORKLOG.md`를 가리키게 했습니다. README에는 깨면 안 되는 제약(빌드 스텝 없음·클래식 스크립트 전역 노출·상대 경로·LF·`lib/valuation.js` 단일 출처), 설계 원칙, 파일 지도, 배치 테스트 사고 이력을 남겼습니다.
- 이유: 작업 로그가 도입되기 직전에 README에 이력을 넣어 두 곳이 겹쳤습니다. 이력이 두 파일에서 각자 자라면 어느 쪽이 최신인지 알 수 없게 됩니다.
- 검증: README·WORKLOG 양쪽에서 이력 항목이 한 번만 나오는 것과 상호 링크를 확인했습니다. 코드 변경이 없어 테스트는 돌리지 않았습니다.
- 남은 일: 아래는 README에서 옮겨온 미해결 항목입니다.
  - `docs/home-screen-spec.md`가 저장소에 추적되지 않고 로컬에만 있습니다. 추적할지 결정이 필요합니다.
  - 한국은행이 2027년 금통위 일정을 아직 공표하지 않았습니다(페이지가 빈 표를 반환). 공표되면 `data/macro-calendar.json`의 일정과 `coverage.BOK`를 갱신합니다.
  - KSIC 세분류(3~5자리) 한글명이 없습니다. 통계청 분류표를 받아 `scripts/lib/ksic.mjs`에 추가하면 됩니다.
  - `pages.yml`의 `workflow_run` 배포 경로를 아직 end-to-end로 관측하지 못했습니다. `push` 경로만 확인됐습니다.
  - `오늘의 주요 뉴스` 카드는 골격만 있습니다. 보유 자산과 연결된 뉴스만 담는다는 경계가 정해져 있습니다.
  - 경제지표 해설 문구와 ECOS·FRED 출처는 미구현입니다. 계열의 `source` 개념과 `<source>/<dataflow>.json` 파일 분리는 미리 해뒀습니다.
- 관련 파일: `README.md`, `WORKLOG.md`

### 2026-08-14 — 작업 공유 로그 도입

- 작업자: Codex
- 변경: README와 분리된 `WORKLOG.md`를 만들고 공통 기록 형식을 정했습니다.
- 이유: Codex와 Claude Code가 번갈아 작업해도 변경 내용과 검증 상태를 빠르게 이어받기 위해서입니다.
- 검증: 파일 생성과 Git 변경 상태를 확인했습니다.
- 남은 일: 이후 모든 기능·설정 변경에서 이 파일을 함께 갱신합니다.
- 관련 파일: `WORKLOG.md`

### 2026-08-13 — 공통 로그아웃 버튼 추가

- 작업자: Codex
- 변경: 공통 왼쪽 내비게이션 하단의 `내 자산` 옆에 로그아웃 아이콘 버튼을 추가했습니다.
- 이유: 어느 주요 화면에서든 설정 화면을 거치지 않고 로그아웃할 수 있게 하기 위해서입니다.
- 검증: 버튼 노출, 접근성 이름, 클릭 후 로그인 화면 이동, 배포 파일 반영을 확인했습니다.
- 남은 일: 없음
- 관련 파일: `lib/shell.js`, `lib/app.css`, `home.html`, `assets.html`, `settings.html`

### 2026-08-12 — 외부 사용자 가입용 이메일 발송 설정

- 작업자: Codex 및 사용자 설정
- 변경: Resend 발신 도메인 `auth.monasset.kr`을 인증하고 Supabase SMTP 연동을 구성했습니다.
- 이유: Supabase 기본 이메일 발송 제한에 막히지 않고 외부 사용자가 회원가입 확인 메일을 받을 수 있게 하기 위해서입니다.
- 검증: Resend 도메인 인증과 SMTP 구성 완료 화면을 확인했습니다.
- 남은 일: 새로운 외부 이메일로 회원가입부터 확인 메일, 로그인까지 종단간 재검증이 필요합니다.
- 관련 파일: Supabase·Resend 외부 설정

### 2026-08-11 — 로그인과 계정별 자산 저장 도입

- 작업자: Codex 및 Claude Code
- 변경: Supabase 이메일 인증, 계정별 세션 저장, 미로그인 접근 차단, 최소 설정 화면을 추가했습니다.
- 이유: 로컬과 배포 사이트 및 다른 기기에서 같은 계정의 자산 데이터를 공유하기 위해서입니다.
- 검증: 로그인 화면 우선 진입과 배포 설정 반영을 확인했습니다. RLS 정책 SQL과 검증 스크립트가 저장소에 있습니다.
- 남은 일: 계정 A와 B를 이용한 실제 RLS 격리 검증 결과를 완료 처리하기 전에 다시 확인해야 합니다.
- 관련 파일: `login.html`, `settings.html`, `lib/account-store.js`, `lib/supabase-config.js`, `supabase/schema.sql`

## 다음 작업자 주의사항

- `lib/valuation.js`와 `lib/portfolio.js`의 계산 로직은 저장 위치와 분리되어 있으므로 계정 저장 작업 때문에 바꾸지 않습니다.
- Supabase `service_role` 키와 SMTP 비밀번호는 브라우저 코드나 저장소에 넣지 않습니다.
- 조회 실패와 자산 0건 상태를 같은 화면으로 처리하지 않습니다.
- 네트워크 저장 실패 시 입력 내용은 로컬에 보존하고 사용자에게 실패를 알립니다.
- 스키마 버전이 달라도 사용자 데이터를 자동 삭제하지 않습니다.
- 현재 작업 트리에 자동 갱신 데이터인 `data/quotes.json`, `data/tickers-kr.json` 변경이 남아 있을 수 있습니다. 작업 범위가 아니면 임의로 포함하거나 되돌리지 않습니다.
