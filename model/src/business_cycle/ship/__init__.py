"""출하 번들 — 앱이 읽을 persist17w 산출물을 한자리에 모은다.

앱은 정적 파일이고 모델은 파이썬이다. 브라우저에서 모델을 돌릴 수 없으므로 결과만
옮기는데, **어느 변형의 결과인지**가 파일 안에 없으면 v1.1과 구분되지 않는다. 그래서
이 단계가 만드는 모든 산출물에 변형 이름이 붙는다.

``realtime``   persist17w 실시간(ALFRED) 경로. 동결 감사와 **같은 열**로 낸다.
``bundle``     현재상태·성숙도·분산 분포를 모아 검증 숫자와 함께 쓴다.

## 무엇을 함께 옮기는가

국면 이름만 옮기면 안 된다는 원칙은 v1.1 때와 같다. 여기에 셋이 더 붙는다.

1. **변형 식별.** 어떤 게이트로 만든 라벨인지. 버전 문자열 하나에 기대지 않는다.
2. **성숙도의 검증 범위.** 확장기에서만 검증됐다는 사실이 신호와 **같은 자리**에 있어야
   한다. 보고서에 적어 두면 화면은 그것을 모른 채 네 국면 모두에 신호를 붙인다.
3. **분산 분포의 묶음.** 네 숫자를 나란히 놓으면 자료가 받쳐 주지 않는 순위가 보인다.

## 트랙 16 전이 게이트는 적용하지 않는다

트랙 21의 2x2가 보였다 — 경계를 고치고 나면 그 게이트는 장기 경로에서 전환을 97에서
96으로 바꿀 뿐이고, 둘을 함께 걸면 판별력 2.421→2.302, 진행률 0.167→0.091로 오히려
나빠진다. 증상 처방이었고 원인은 경계였다. 이 단계는 `transition_gate`를 부르지 않는다.
"""
