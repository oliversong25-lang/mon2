"""전이 게이트. 동결 v1.1 위에 얹는 **병렬 변형**이며 v1.1을 고치지 않는다.

왜 병렬 변형인가 — v1.1은 설정 지문으로 잠겨 있고 `developmentStopped`다. 게이트는
점수를 하나도 다시 계산하지 않고 **이미 나온 주간 경로에서 어느 전이를 받아들일지만**
정한다. 그래서 순수 후처리로 두면 v1.1은 바이트 단위로 그대로 재현되고, 같은 입력에
두 출력을 나란히 놓고 비교할 수 있다. 엔진 모수를 바꾼 v1.2를 만들면 그 비교가
불가능해진다.

풀려는 문제는 채터링이다. 688주에 전이 72회 — 9.6주에 한 번이고 그중 64회가
`expansion`↔`slowdown` 왕복이다. 폭·지속 게이트가 있는 두 국면(침체·회복)은 안정적이고
없는 두 국면은 불안정하다. 모델 자신의 출력 안에 있는 자연 실험이다.

지연과 채터링은 같은 다이얼 위에 있다. 게이트를 조이면 왕복이 줄고 인식이 늦어진다.
이 패키지의 산출물은 임계값 하나가 아니라 **그 교환을 드러내는 표**다.
"""
