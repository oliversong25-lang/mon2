# 자산 입력 · 홈 화면

개인 자산관리 웹앱(AssetFlow)입니다. `oliversong25-lang/mon2` · 배포 `https://oliversong25-lang.github.io/mon2/` (커스텀 도메인 `monasset.kr`).

## 이 저장소에서 작업하기 전에

여러 도구(Claude Code · Codex)가 번갈아 이 저장소를 만집니다. **이 README가 도구 사이의 인수인계 문서입니다** — 구조를 바꾸거나 판단이 필요한 결정을 내렸으면 그 변경과 함께 여기에 적습니다. 특히 "왜 이렇게 했는지"를 적습니다. 코드만 보면 되돌리기 쉬운 결정들이 대부분이고, 실제로 여기 적힌 내용의 상당수는 한 번 잘못 만들었다가 고친 것들입니다.

### 브랜치 운영

- **`main`이 유일한 작업·통합 브랜치입니다.** 앱 화면, 저장 구조, 배치 산출물처럼 사용자가 보게 될 변경은 `main`에 반영된 상태로 끝냅니다.
- `model/*` 브랜치는 검증 트랙의 연구 이력을 보존하는 읽기 전용 기록입니다. 새 작업을 이어 붙이거나 앱 변경을 병합 대기 상태로 두지 않습니다.
- GitHub Pages는 `main`의 저장소 루트를 그대로 배포합니다. 브랜치에서만 맞는 결과는 배포 완료가 아니며, 앱이 읽는 배치 산출물도 `main`에 커밋합니다.

### 깨면 안 되는 제약

- **빌드 스텝이 없습니다.** 순수 정적 HTML/CSS/JS입니다. 번들러·프레임워크·트랜스파일러·런타임 백엔드를 들이지 않습니다. GitHub Pages가 저장소 루트를 그대로 서빙합니다.
- **`lib/*.js`는 클래식 스크립트이고 전역으로 노출합니다** (`window.Valuation`·`Portfolio`·`SessionStore`·`Shell`·`Format`·`Indicators`). ES 모듈로 바꾸면 `asset-input.html`이 의존하는 전역 스코프와 회귀 테스트의 `page.evaluate` 경로가 함께 깨집니다. 반대로 `scripts/*.mjs`(배치)는 ESM입니다.
- **경로는 전부 상대 경로**입니다. 절대 경로(`/data/...`)를 쓰면 하위 경로 배포(`/mon2/`)에서만 깨져 로컬에서는 안 보입니다.
- **줄바꿈은 LF**입니다(`.gitattributes`의 `* text=auto eol=lf`). Windows에서 작업해도 커밋에는 LF로 들어갑니다.
- **`lib/valuation.js`가 평가금액의 유일한 출처**입니다. 화면마다 평가 로직을 따로 두면 한쪽만 고쳐지는 순간 같은 자산이 다른 금액으로 보입니다.

### 관통하는 원칙: 실패가 성공처럼 보이면 안 된다

이 저장소의 설계 판단 대부분이 여기서 나옵니다.

- 조용한 0, 빈 화면, 뭉뚱그린 "없습니다" 금지. **모르면 `null`을 반환하고 화면이 이유를 말합니다.**
- 환율을 모를 때 `|| 1`로 넘기지 않습니다(USD 2,000이 2,000원이 됩니다).
- 데이터가 없어도 200을 주는 API가 여럿입니다(CoinGecko·OECD SDMX·DART). **0건을 성공으로 넘기지 않습니다.**
- 검증할 수 없는 값은 **지어내지 않고 제품 안에서 공백을 드러냅니다**(KSIC 세분류명·금통위 2027 일정이 그 예입니다).
- 배치 산출물은 임시 파일에 쓰고 검증을 통과해야 원자적으로 교체합니다. 실패하면 기존 파일을 보존합니다.

### 구조 한눈에

```
*.html              화면 (index · login · asset-input · home · assets · indicators · settings)
lib/*.js            브라우저 런타임 (클래식 스크립트 · 전역 노출)
scripts/*.mjs       배치와 회귀 테스트 (ESM · Node 20)
scripts/lib/*.mjs   배치가 쓰는 외부 API 클라이언트
data/*.json         배치 산출물. 앱은 이걸 fetch만 합니다
supabase/schema.sql 계정·RLS
.github/workflows/  배치 5개 (quotes · tickers · indicators · daily-rates) + pages 배포
```

| 배치 | 주기 | 산출물 |
|---|---|---|
| `quotes.yml` | **수동 (2026-09-05부터 예약 중지)** | `data/quotes.json` |
| `indicators.yml` | 매일 15:00 KST | `data/indicators/**` |
| `daily-rates.yml` | 매일 2회 | `data/indicators/daily/**` |
| `tickers.yml` | 매주 일요일 05:00 KST | `data/tickers-*.json` |
| `pages.yml` | 위 배치 완료 시 + push | 배포 |

`quotes.yml`은 data.go.kr이 Actions 러너를 막아 예약을 껐습니다 — 아래 [국내 시세 배치](#국내-시세-배치-dataquotesjson) 참고. 국내 IP에서 `npm run quotes:local`로 돌립니다.

브라우저 보안 정책 때문에 HTML을 `file://`로 직접 열면 `data/*.json` 로드가 차단될 수 있습니다. 저장소 루트에서 로컬 서버를 실행한 뒤 접속하세요.

```powershell
python -m http.server 4174 --bind 127.0.0.1
```

- 진입점: `http://127.0.0.1:4174/` — 로그인 뒤 계정 데이터를 불러와 홈 또는 자산 입력으로 보냅니다
- 로그인: `http://127.0.0.1:4174/login.html`
- 홈 화면: `http://127.0.0.1:4174/home.html`
- 자산군별: `http://127.0.0.1:4174/assets.html`
- 자산 입력: `http://127.0.0.1:4174/asset-input.html`

## 배포 (GitHub Pages)

**최초 1회 수동 설정이 필요합니다.** 저장소 Settings → Pages → "Build and deployment" → Source를 **GitHub Actions**로 바꾸세요. 이걸 켜기 전까지는 워크플로가 `configure-pages` 단계에서 멈춥니다. 자동화를 시도했지만(`enablement: true`) Pages 사이트 생성 API가 저장소 관리자 권한을 요구하고 워크플로의 `GITHUB_TOKEN`에는 그 권한이 없어 실패했습니다.

설정을 켠 뒤에는 `main`에 푸시되면 `.github/workflows/pages.yml`이 저장소 루트를 그대로 Pages에 올립니다. 배포 주소는 `https://oliversong25-lang.github.io/mon2/`(커스텀 도메인 `monasset.kr`)입니다.

**배치 결과는 `push`가 아니라 `workflow_run`으로 배포됩니다.** 기본 `GITHUB_TOKEN`으로 만든 커밋은 다른 워크플로를 트리거하지 않습니다(GitHub이 무한 루프를 막으려고 걸어 둔 제약). 그래서 배치는 매일 성공적으로 커밋하는데 사이트는 갱신되지 않았습니다 — 실측으로 저장소 HEAD는 `asOf 2026-08-12`, `monasset.kr/data/quotes.json`은 `asOf 2026-08-07`(마지막 사람 커밋 시점)이었습니다. `workflow_run`은 커밋 이벤트가 아니라 "워크플로가 끝났다"는 이벤트라 그 제약을 받지 않습니다.

배치에서 `workflow_dispatch`를 부르는 방식은 같은 제약에 걸려 별도 PAT가 필요하므로 쓰지 않습니다. Pages 소스를 브랜치 배포로 바꾸는 것도 하지 않습니다 — 유료로 붙여 둔 커스텀 도메인이 걸려 있습니다.

실패한 배치의 결과는 배포하지 않습니다(`workflow_run`은 성공·실패를 가리지 않고 발화하므로 잡에서 `conclusion == 'success'`를 확인합니다). concurrency 그룹이 서로 달라(`pages` / `quotes-batch` / `tickers-batch`) 세 워크플로가 막지 않고, 두 배치가 비슷한 시각에 끝나면 마지막 배포만 남습니다.

경로는 전부 상대 경로라 하위 경로(`/mon2/`)에서도 그대로 동작합니다 — 절대 경로(`/data/...`)를 새로 쓰면 배포에서만 깨집니다.

**공개되는 것은 앱 코드·공개 시세 데이터·Supabase anon key입니다.** anon key는 브라우저 앱에서 쓰도록 설계된 공개 키이며, `supabase/schema.sql`의 RLS가 계정 데이터를 격리합니다. `service_role` key는 클라이언트와 저장소에 절대 두지 않습니다. 시세 배치용 비밀 키는 계속 GitHub Actions Secrets에서만 씁니다.

## 로그인과 계정별 저장

1. Supabase 프로젝트의 SQL Editor에서 `supabase/schema.sql`을 실행합니다.
2. Authentication → URL Configuration에 로컬 주소와 Pages 주소를 Redirect URL로 등록합니다.
3. `lib/supabase-config.js`의 `url`과 `anonKey`를 프로젝트의 URL·anon key로 바꿉니다. 이 파일에 service_role key를 넣지 않습니다.
4. 로컬 서버에서 `login.html`을 열어 이메일 회원가입·로그인을 확인합니다.

현재 `assetInput.session` 문서 전체를 사용자별 `user_asset_sessions` 한 행의 `payload`에 저장합니다. 자산 배열·입력 진행 상태·스냅샷을 한 번에 교체해야 서로 다른 시점의 값으로 갈라지지 않고, 기존 `lib/valuation.js`와 `lib/portfolio.js`를 수정하지 않아도 되기 때문입니다. 스키마 번호는 별도 열과 payload 양쪽에 유지하며, 버전이 달라도 기존 `SessionStore.classify`가 자산을 보존하고 안내합니다.

저장은 **로컬 즉시 저장 + Supabase 800ms 디바운스 저장**입니다. 네트워크가 끊기면 계정별 pending 사본을 localStorage에 남기고 화면 하단에 실패를 알립니다. 연결이 돌아오면 자동 재시도합니다. 따라서 키 입력마다 네트워크를 호출하지 않으면서도 화면 이동이나 통신 실패로 입력을 잃지 않습니다.

RLS는 SQL 존재 여부가 아니라 실제 두 계정으로 검증합니다. 아래 환경 변수에는 테스트 전용 계정을 사용하세요.

```powershell
$env:SUPABASE_URL="https://<project>.supabase.co"
$env:SUPABASE_ANON_KEY="<anon-key>"
$env:RLS_TEST_A_EMAIL="account-a@example.com"
$env:RLS_TEST_A_PASSWORD="..."
$env:RLS_TEST_B_EMAIL="account-b@example.com"
$env:RLS_TEST_B_PASSWORD="..."
npm run test:rls
```

검증은 A의 행·스냅샷 저장, A의 본인 조회, B의 A 행 조회 0건, B가 A의 user_id로 행을 쓰려는 시도 차단까지 수행합니다. URL·키·두 테스트 계정이 없으면 성공으로 가장하지 않고 종료 코드 1로 실패합니다.

종단간 암호화는 이번 범위 밖입니다. 확장할 때는 키 복구 정책, 비밀번호 분실 시 데이터 복구 불가 위험, 기기별 키 동기화를 먼저 결정해야 합니다.

### 부분 갱신과 날짜가 섞인 데이터

국내 주식·ETF·ETN·금·환율은 핵심이라 하나라도 실패하면 배치를 실패시킵니다. **가상자산(CoinGecko)만 예외**입니다 — 제3자 API 한 곳이 흔들린다고 그날 국내 시세 전체를 버리는 건 손실이 더 큽니다. 실패하면 직전 코인 값을 그대로 들고 가고, 경고를 남기고, 국내 갱신은 그대로 게시합니다. 직전 값조차 없을 때만 실패입니다.

그러면 산출물 안에서 날짜가 갈립니다. **날짜가 섞인 데이터를 한 시점의 스냅샷인 것처럼 보여주면 안 되므로** `cryptoAsOf`를 따로 적고, 그 값이 `asOf`보다 이르면 홈 화면의 `확인이 필요한 항목` 줄에 이렇게 뜹니다.

```
가상자산 시세가 2026-08-04 기준이에요 (나머지보다 3일 이전)
코인게코 갱신에 실패해 직전 값을 그대로 쓰고 있어요. 국내 시세는 정상 갱신됐습니다
```

### 네트워크 오류와 재시도

모든 외부 호출은 `scripts/lib/http.mjs`를 지납니다. 2026-08-12 예약 실행이 `[시세 배치 실패] fetch failed` 한 줄만 남기고 죽었는데, undici가 던지는 이 메시지에는 URL이 없어 어느 API가 끊긴 건지 로그만으로는 알 수 없었고 재시도도 없어 한 번의 순간적인 실패가 그날 데이터를 통째로 날렸습니다.

이제 모든 네트워크 오류는 `[API 이름] 사유 (URL)` 형태로 남고, 네트워크 오류·5xx·429는 0.8초부터 두 배씩 늘려가며 재시도합니다(4xx는 우리가 잘못 부른 것이므로 즉시 포기). 중간 시도 실패도 URL과 함께 남깁니다 — 마지막 예외에만 붙이면 재시도 끝에 성공한 날에는 어디가 불안정했는지가 기록에서 사라집니다. URL의 인증키는 `***`로 가립니다(공개 저장소의 Actions 로그로 새어 나갑니다).

## 경기국면 (미국 4국면 모델)

홈 화면의 경제 칸과 분석 탭이 같은 데이터를 씁니다. 지표를 여러 줄 늘어놓는 대신
**지금 미국 경제가 네 국면(회복기·확장기·후퇴기·침체기) 중 어디인지** 하나를 말합니다.

### 이 모델이 하는 일과 하지 않는 일

- **합니다** — 현재 국면을 주 단위로 서술합니다.
- **하지 않습니다** — 침체 예측, 시점 예측, 투자 판단. 모델이 만들지 않으므로 화면도 만들지 않습니다.

상태는 `provisional`이고 **최종 검증이 아닙니다.** 그래서 데이터와 화면 양쪽에서
국면 이름과 함께 증거 품질·회복 인식 지연·한계를 항상 같이 싣습니다. 이건 취향이 아니라
계약이라, 하나라도 빠지면 `build:business-cycle`이 실패하고 `test:business-cycle`이 잡습니다.

### 데이터 흐름

모델은 파이썬이고 앱은 정적 파일입니다. 브라우저에서 모델을 돌릴 수 없으므로 다른
배치와 같은 방식으로 **결과만** 옮깁니다.

```powershell
npm run build:business-cycle                       # 기본: ../mon2-bc/model/outputs
npm run build:business-cycle -- --model-outputs=<경로>
```

| 산출물 | 내용 |
|---|---|
| `data/business-cycle/us.json` | 현재 국면 + 688주 주간 경로 + 한계 + 출처 (약 144KB) |

모델 저장소는 `model/*` 브랜치에서 개발되었고 산출물만 main에 커밋됩니다.
모델을 다시 돌린 날에만 이 배치를 실행하면 됩니다.

### 화면에서 읽는 법

- **증거 품질 `낮음`** — 국면이 틀렸다는 뜻이 아닙니다. 지금 증거로는 국면 사이 거리가
  뚜렷하지 않다는 뜻입니다. 모델이 저장한 필드를 그대로 보여주며 화면에서 올리지 않습니다.
- **판정 보류** — 동행 지표 발표가 멈춰 국면을 말할 근거가 없는 주입니다. 빈칸으로 때우지
  않고 보류라고 씁니다. 원시 측정값은 그대로 남습니다.
- **국면 순서** — 정해진 순서로 돌지 않습니다. 후퇴기에서 확장기로 재가속하거나 중간
  국면을 건너뛸 수 있고, 그건 오류가 아닙니다.
- **회복 인식 지연 `AMBER`** — 저점 이후 회복 인식이 최대 11주까지 늦을 수 있습니다.
  이 모델은 저점을 실시간으로 짚지 못합니다.

## 경제지표 (OECD)

키가 필요 없는 OECD SDMX 공개 API에서 매일 받아 `data/indicators/` 아래 정적 JSON으로 굽습니다. 런타임 백엔드는 없습니다.

**지표 50개 · 국가 55개 · 계열 2,064건**을 7개 갈래(물가·고용·경기·생산·금리·금융·무역·성장)로 나눠 담습니다.

### 왜 전 계열을 담지 않는가

공개 엔드포인트에는 **dataflow가 1,546개** 있습니다. 그중 거시·금융과 관련된 22개만 추려도 계열이 12만 개이고, 10년치를 전부 담으면 **150MB가 넘습니다** — 매일 배치가 그만큼을 커밋하게 됩니다.

그래서 계열을 통째로 받지 않고 **헤드라인만 selector로 집습니다**. 조정치·연령대별·품목별 분해는 담지 않습니다. 실측 규모는 이렇습니다.

```
DF_IALFS_INDIC     40,238계열   실업률을 연령×성별×교육수준으로 쪼갠 것 — 헤드라인 한 줄이면 된다
DF_PRICES_ALL      15,986계열   품목(COICOP) 29개 × 변환 4종 × 조정 2종
DF_TABLE1_EXPEND   19,225계열   차원 13개. GDP 한 줄이 필요하면 DF_KEI에 이미 있다
DF_RHPI_TARGET      2,448계열   REF_AREA가 국가가 아니라 지역 377개 — 국가 축이 무너진다
DF_OIALAB_INDIC       373계열   최신 관측이 2024-02. 2년 넘게 멈춰 있다
```

### 파일 구조 (3단 분할)

| 파일 | 크기 | 언제 받나 |
|---|---|---|
| `index.json` | 26KB | 항상. 갈래·지표·국가 목록과 (지표×국가) 존재 여부, 헤드라인 값 |
| `latest/<갈래>.json` | 22~40KB × 7 | 그 갈래를 열 때. 최신값 1개씩 |
| `oecd/<dataflow>.json` | 합계 4.2MB | 지표를 펼칠 때. 전 국가 10년 시계열 |

지표가 7개에서 50개로 늘면서 최신값까지 한 파일에 담으면 250KB가 됩니다. 홈 화면은 네 줄만 필요한데 그걸 위해 매번 250KB를 받는 것은 낭비라 **헤드라인 값만 `index.json`에 얹었습니다**. 반대로 검색은 전 계열을 훑어야 하므로 이름은 `index.json`에 빠짐없이 들어 있습니다 — **기본 화면에 열려 있지 않은 지표도 검색으로 찾힙니다**(회귀 테스트가 이걸 검사합니다).

### API를 만지며 확인한 것들

문서가 아니라 실제 응답을 보고 맞춘 것들이라 적어 둡니다.

- **`Accept-Language` 헤더가 없으면 500 `languageTag1`** 이 옵니다. Accept만 맞춰도 안 됩니다.
- **키는 자리 수가 정확해야** 합니다. 모자라면 422 `Not enough key values in query, expecting 9 got 1` — 다행히 필요한 자리 수를 오류가 알려줍니다.
- **버전도 맞아야 합니다.** 전부 `1.0`으로 두면 STES 계열이 통째로 404납니다(`DF_CLI`은 4.1, `DF_INDSERV`는 4.3).
- `dimensionAtObservation=AllDimensions`로 부르면 관측 키가 **차원 값 배열의 인덱스를 콜론으로 이은 문자열**이 됩니다(`"8:0:4:0:1:0:0:0:0:0"`). 값은 배열이고 `[0]`이 관측값입니다.
- **차원 순서는 dataflow마다 다릅니다.** `DF_CLI`은 FREQ가 1번이지만 `DF_IALFS_UNE_M`은 8번입니다. 그래서 파서는 위치가 아니라 id로 찾습니다.
- **데이터가 없어도 HTTP 200에 `errors: []`이고 `observations`만 `{}`로 빕니다.** 상태 코드만 보면 성공으로 읽히므로 0건을 성공으로 넘기지 않습니다.
- **분당 호출 한도가 있습니다.** dataflow 호출이 4번에서 13번이 되자 다섯 번째부터 전부 429가 났습니다(13개 중 8개 실패). 재시도로 뚫는 대신 `scripts/lib/oecd.mjs`가 **호출 간격을 8초로 벌립니다** — DART에서 같은 문제를 같은 방법으로 풀었습니다. 회귀 테스트는 `OECD_MIN_GAP_MS=0`으로 이 대기를 끕니다(안 끄면 한 번 도는 데 5분을 넘깁니다).

**선택자는 반드시 실제 코드리스트를 보고 고릅니다.** 추측한 값은 예외가 아니라 관측 0건으로 조용히 사라집니다. 실제로 첫 실행에서 세 지표가 그렇게 비었고, 원인은 각각 이랬습니다.

| 지표 | 틀린 이유 |
|---|---|
| 제조업 수주잔량 | `OB`는 `TIME_HORIZ=C`로만 있고 한국이 제출하지 않습니다. 한국을 포함하는 수주 유입(`OI`)으로 바꿨습니다 |
| 상품수지·서비스수지 | GDP 대비(`PT_B1GQ`)는 경상수지에만 있습니다. 이 둘은 달러 금액(`USD_EXC`)으로만 발행됩니다 |

### 배치 동작

대부분의 지표는 월간·분기라 매일 돌려도 바뀌는 게 없습니다. **그건 정상이고 오류가 아니라서** 로그가 "갱신 N · 변동 없음 M"으로 나눠 찍습니다. 모든 계열은 관측 기간(`period`)을 배치 실행 시각과 별개로 들고 다닙니다.

한 계열이 실패해도 나머지는 갱신합니다. 실패는 어느 엔드포인트인지 이름을 달고 `index.failures`와 로그에 남으며, **수집된 계열이 0건이면 성공으로 쓰지 않고** 기존 파일을 보존합니다.

**전체 소요는 실측 105~120초**입니다(dataflow 13개 × 8초 간격 + 요청당 3~10초). Actions 창에 여유가 커서 요일 분산은 두지 않았습니다 — 매일 13개를 다 받습니다.

한글 이름이 없는 지역 코드는 **건너뛰고 몇 개를 건너뛰었는지 이름과 함께 찍습니다**. 기계번역으로 지어내지 않습니다.

### 화면

경제지표 탭은 **갈래가 1차 축**입니다 — 들어오면 물가·고용·경기·생산·금리·금융·무역·성장이 먼저 보이고, 갈래 안에 지표가, 지표를 펼치면 국가가 있습니다. 국가는 어느 단계에서나 거는 필터이고, 한 나라의 전체 그림을 보고 싶은 사람을 위해 **국가 우선 피벗**(`국가별` 보기)도 남겨 뒀습니다. 두 경로는 같은 계열에 닿습니다.

검색은 `index.json`만으로 되므로 화면에 열려 있지 않은 지표도 찾힙니다. 지표명·국가명을 한글과 영문 양쪽에서 봅니다.

**모든 값에 관측 기간과 주기가 함께 붙습니다.** 3개월 전 값은 낡은 게 아니라 그 시점의 정상 데이터이고, 기간을 안 적으면 사용자가 언제 값인지 추측해야 합니다.

값이 아직 안 온 상태와 값이 0인 상태는 다릅니다. 주제 파일이 도착하기 전에는 **0이 아니라 자리표시**를 둡니다.

홈 카드는 헤드라인 네 줄만 올리고 나머지는 더보기로 경제지표 탭에 넘깁니다. 어느 지표를 올릴지는 `scripts/lib/indicators.mjs`의 `headline` 플래그가 정하고, 그 값이 `index.json`의 `headlineSeries`에 실려 와서 **홈은 갈래 파일을 받지 않아도 그려집니다**.

카탈로그가 없거나, 비었거나, 가장 최신 관측이 6개월을 넘게 뒤처지면 **빈 화면이나 0 대신 무엇이 없는지 밝힙니다**.

출처 표기는 OECD가 요구합니다(CC BY 4.0). 홈과 경제지표 탭 양쪽 출처 줄에 넣었습니다.

해설·활용방안 문구는 아직 넣지 않았습니다. ECOS는 다음 차례입니다 — 계열 id가 `<출처>:<지표>:<국가>`이고 지표 정의에 `source`가 따로 있어, `scripts/lib/indicators.mjs`에 source가 다른 배열을 이어 붙이면 기존 파일 모양을 바꾸지 않고 들어옵니다.

## 배치 산출물과 캐시

앱이 받는 데이터 파일은 전부 `lib/fetch-data.js`를 지납니다. 파일마다 갱신 주기가 달라 캐시 정책도 다릅니다.

| 파일 | 배치 갱신 주기 | 캐시 결정 |
|---|---|---|
| `data/quotes.json` | 매일 | 조건부 요청 (`cache: "no-cache"`) |
| `data/indicators/index.json` | 매일 | 조건부 요청 |
| `data/indicators/latest/*.json` | 매일 | 조건부 요청 |
| `data/indicators/oecd/*.json` | 매일 | 조건부 요청 |
| `data/macro-calendar.json` | 손으로 유지 | 조건부 요청 |
| `data/tickers-{kr,us,global}.json` | 주 1회 · 합계 4MB | **기본 캐시** |

**왜 필요했나.** 배치는 커밋했고 Pages도 배포했는데 브라우저가 어제 파일을 들고 있었습니다 — 실측으로 같은 경로에 그냥 요청하면 asOf 2026-08-12, 캐시를 비껴가면 2026-08-13이 왔습니다(저장소와 서버는 둘 다 08-13). 화면에는 배치가 고장 난 것과 **똑같이** 보여서 파이프라인 쪽을 며칠 뒤졌습니다. 코드 파일은 이미 `?v=20260814-1` 같은 표식을 달고 있었는데, 정작 매일 바뀌는 데이터 파일에는 아무것도 없었습니다.

**왜 `cache: "no-cache"`인가.** 빌드 스텝이 없는 정적 사이트이고 데이터는 코드 변경 없이 바뀝니다. 그래서 HTML에 박아 두는 `?v=` 버전 문자열로는 데이터 갱신을 따라갈 수 없습니다(배치가 HTML을 고치지 않습니다). `?t=Date.now()`는 캐시를 확실히 비껴가지만 매 로드가 전량 재다운로드가 되고, CDN 입장에서는 매번 다른 URL이라 캐시가 아예 서지 않습니다. `no-cache`는 캐시를 끄는 것이 아니라 **매번 서버에 물어보게** 하는 것이라, 내용이 그대로면 304가 오고 본문은 다시 받지 않습니다.

**캐시에서 나왔는지 구별합니다.** Resource Timing의 `transferSize`가 0이면 네트워크를 타지 않은 것입니다. 시세가 이틀 이상 낡았는데 그 값이 캐시에서 나왔다면 홈 화면이 그 사실을 따로 적습니다 — "배치가 멈춘 것"과 "내 브라우저가 옛 사본을 들고 있는 것"은 원인도 대처도 다른데 화면에는 똑같이 보이기 때문입니다.

## 시세 로딩 중 표시 규칙

**시세가 도착하기 전에는 금액을 아예 표시하지 않습니다.** 카드 골격과 자리표시(`.skel`)만 띄웁니다.

예전에는 `quotes.json` 로드 전에 한 번 렌더하고 로드 후 다시 렌더해서, 환율이 없는 동안 외화가 빠진 총자산이 보였다가 정정됐습니다(실측 6억 2,350만원 → 6억 5,866만원, 3,516만원 차이). 금액이 깜빡이며 바뀌면 어느 값을 믿어야 할지 알 수 없고, 네트워크가 느리면 틀린 값이 더 오래 보입니다. 더 나쁜 건 `시세를 확인하지 못했어요`라는 사실 아닌 경고가 떴다 사라진 것입니다 — 그 문구는 정말로 시세를 못 받았을 때의 신뢰 장치라 그렇게 닳으면 안 됩니다.

같은 이유로 환율을 모르는 동안 `rates[통화] || 1`로 계산하지 않습니다. 1:1로 치면 USD 2,000이 2,000원으로 둔갑합니다. 모르면 숫자를 만들지 않고 `환율 확인 중`을 표시합니다.

**PC 우선입니다.** 1440px를 기준으로 만들었고, 레이아웃은 확정된 목업(4안 1페이지 · 7안)을 기준선으로 삼았습니다. 모바일 대응은 범위 밖이라 좁은 폭에서는 단을 1열로 무너뜨려 깨지지만 않게만 해두었습니다.

## 화면 구조

```
홈 (home.html)          전체 자산 현황 + 경기국면 카드
├ 자산 (assets.html)     현금 · 예적금 · 주식·ETF · 펀드 · 채권 · 가상자산 · 원자재 · 부동산
├ 경제지표 (indicators.html)  OECD 43개국 × 7지표
└ 분석 (analysis.html)    미국 4국면 모델 상세
```

| 파일 | 역할 |
|---|---|
| `home.html` | 홈 — 총자산·구성·요약·추이·보유 자산 |
| `assets.html` | 자산군 탭 8개. 이번 단계는 껍데기(보유 목록·평가금액·비중)까지 |
| `indicators.html` | 경제지표 탭 (국가 우선 · 한/영 검색) |
| `analysis.html` | 분석 탭 — 미국 4국면 모델 상세 (현재 국면·근거·경로·도메인·한계) |
| `asset-input.html` | 자산 입력 (8개 자산군) |
| `lib/app.css` | **다크 네이비 테마와 셸의 단일 출처**. 색·간격·카드·표 |
| `lib/shell.js` | 좌측 내비게이션 (홈/자산/경제지표/분석·목표 관리·알림·설정) |
| `lib/session.js` | **세션 읽기·로컬 우선 저장의 단일 출처**. 저장 키·스키마 번호·마이그레이션 |
| `lib/account-store.js` | Supabase 인증·계정 세션 조회·오프라인 대기열·원격 저장 |
| `lib/auth-guard.js` / `lib/supabase-config.js` | 로그인 가드 / 프로젝트 URL·anon key |
| `login.html` / `settings.html` | 이메일 로그인·회원가입 / 로그아웃 |
| `lib/valuation.js` | **평가금액의 단일 출처**. 시세 로딩·환율 환산·자산별 원화 평가 |
| `lib/portfolio.js` | 집계와 분류(금융/실물 · 원금 보장 · 현금화 · 성장 가능성) |
| `lib/indicators-view.js` | `catalog.json` 로딩·검색·시계열 조회. 경제지표 탭이 씁니다 |
| `lib/business-cycle-view.js` | 4국면 모델 결과 로딩·국면/증거품질/신선도 표기. **홈 카드와 분석 탭이 공유** |
| `lib/format.js` | 금액·비율 표기. 같은 금액이 화면마다 다르게 반올림되면 안 됩니다 |

배치 쪽은 이렇게 나뉩니다.

| 파일 | 역할 |
|---|---|
| `scripts/lib/http.mjs` | **모든 외부 호출이 지나는 곳**. 재시도·백오프·엔드포인트 이름·키 마스킹 |
| `scripts/lib/data-go-kr.mjs` | 공공데이터포털 (주식·ETF·ETN·금 시세, 상장종목) · 종목코드 정규화 |
| `scripts/lib/dart.mjs` | OPEN DART 기업개황(업종) · 분당 유량 제한 |
| `scripts/lib/ksic.mjs` | 한국표준산업분류 중분류 표 |
| `scripts/lib/crypto.mjs` | CoinGecko · 심볼→코인 ID 매핑 표 |
| `scripts/lib/oecd.mjs` | OECD SDMX 클라이언트 · 관측 디코딩 |
| `scripts/lib/indicators.mjs` | dataflow·지표 선택자·국가 한글명 표 |
| `scripts/build-macro-calendar.mjs` | 거시 일정 생성기. **외부 호출 없음** — 손으로 적은 날짜의 시간대 환산·시나리오 부착·검증만 한다 |

`assets.html`은 자산군마다 내용이 크게 달라질 예정이라, 탭 정의(`GROUP_TABS`)와 본문 렌더(`groupBody`)·열 정의(`COL`·`columnsFor`)를 분리해 뒀습니다. 자산군별 화면을 채울 때는 `groupBody`에 분기를, 열이 달라지면 `columnsFor`에 줄을 더하면 됩니다. 내비의 설정은 로그아웃만 제공하며, `분석`·`목표 관리`·`알림`은 아직 "준비 중"입니다.

좌측 내비의 `자산`은 이동 대신 하위 목록을 펼칩니다. 보유 건수가 0인 자산군도 목록에 남깁니다 — 보유하지 않은 자산군을 숨기면 무엇을 더 넣을 수 있는지 알 수 없고, 목록 자체가 입력 안내 역할을 합니다.

### 자산군 선택은 관문이 아니라 필터

`asset-input.html` 첫 화면의 자산군 선택은 **먼저 볼 순서를 고르는 것**이지 무엇을 넣을 수 있는지를 정하는 것이 아닙니다. 예전에는 고른 자산군만 순회했고 다시 들어와도 그 안에서만 추가할 수 있어서, 처음에 현금만 고른 사용자에게는 주식·가상자산으로 가는 버튼이 **아예 없었습니다**. 위의 "보유 건수가 0인 자산군도 목록에 남긴다"와 정면으로 어긋나 있었고, 좁혔던 이유(처음 쓰는 사용자의 부담)는 2026-08-11 자기 사용 우선 검토에서 이미 걷어낸 근거였습니다.

지금은 `orderedGroups()`가 걸어갈 순서를 만듭니다 — 고른 자산군이 앞, 나머지가 뒤, 언제나 8개 전부. 검토 화면(`review()`)이 8개를 모두 싣고 0건인 자산군은 `＋ 추가` 한 줄로 바로 열립니다(접힌 `<details>` 안에 두면 버튼이 화면에 보이지 않아 도달 경로를 되살린 의미가 없습니다). 선택 화면은 검토 화면의 `먼저 볼 자산군 다시 고르기`로 다시 열리고, 선택 화면에서는 `검토 화면으로`로 돌아옵니다 — 한 번 물은 것은 다시 물을 수 있어야 합니다.

**체크를 푸는 것은 삭제가 아닙니다.** 자산 레코드는 그대로 남고 평가·총자산에도 그대로 들어갑니다. 조용히 사라지지 않도록 선택 화면이 그 자리에서 "선택에서 뺀 현금 1건은 지우지 않았습니다. 검토 화면과 전체 자산에 그대로 남고 총자산에도 들어갑니다"라고 적고, 자산군 줄에는 `1건 등록됨` 배지가 체크 여부와 무관하게 남습니다. 스키마가 달라도 사용자 자산을 지우지 않는다는 규칙과 같은 자리입니다.

`SESSION_SCHEMA`는 **올리지 않았습니다**(v7 그대로). `selectedGroups`는 언제나 `GROUPS` 순서로 다시 만들어지므로 `orderedGroups()`의 앞부분이 곧 `selectedGroups`이고, `currentGroupIndex`가 가리키는 대상이 예전 세션과 정확히 같습니다. 저장 형식이 바뀌지 않았으니 백업·이관도 필요 없습니다.

`assets.html`의 0건 탭에는 이미 `<자산군> 추가` 버튼이 있지만 `asset-input.html#add`로만 보냅니다 — 자산군을 지목하지 않으므로 검토 화면에서 한 번 더 골라야 합니다. `#group=<id>`를 받아 그 자산군을 바로 여는 것이 자연스럽지만, 그건 별도 작업으로 둡니다.

### 자리만 잡아둔 칸

`오늘의 주요 뉴스`·`주요 경제지표` 두 칸은 골격과 `준비 중` 표시만 있고 데이터 연동은 다음 단계입니다. `다가오는 일정`은 아래처럼 실제로 동작합니다.

`오늘의 주요 뉴스`는 **일반 경제 뉴스가 아니라 보유 자산과 연결된 뉴스만** 담습니다. 삼성전자를 보유하면 삼성전자 뉴스, 달러 자산이 있으면 환율 뉴스. 이 앱은 경제를 공부시키는 도구가 아니라 본인 돈을 이해하게 만드는 도구이므로, 보유와 무관한 뉴스를 채우면 그 순간 다른 종류의 앱이 됩니다.

### 다가오는 일정 (거시 경제 일정)

자산 만기가 아니라 **거시 경제 일정**입니다. 자산 만기는 총자산 아래의 `확인이 필요한 항목` 줄이 맡습니다 — 이 경계를 섞지 마세요.

범위는 **한국·미국의 금리 결정과 주요 지표 발표**입니다.

| 나라 | 담는 일정 |
|---|---|
| 한국 | 금통위 기준금리 · 소비자물가동향 · 고용동향 · 국민소득(속보·잠정) · 경상수지 · 수출입 동향 |
| 미국 | FOMC · CPI · PCE · 고용보고서 · GDP · 소매판매 · ISM 제조업/서비스업 |

각 일정은 `category`(rate·inflation·employment·growth·trade), `country`(ISO-3), `importance`(high·medium)를 함께 들고 다닙니다.

API를 붙이지 않았습니다. 이 일정들은 기관이 1년 단위로 미리 공표하므로 정적 파일이 맞는 출처입니다. 다만 파일을 손으로 쓰지 않고 **`scripts/build-macro-calendar.mjs`가 생성**합니다 — 날짜는 그 스크립트의 표에 손으로 적혀 있고(각 표 위에 기관별 공표 페이지 주소가 있습니다), 스크립트가 하는 일은 시간대 환산과 시나리오 부착·검증뿐입니다. 외부 API는 부르지 않습니다.

```powershell
node scripts/build-macro-calendar.mjs
```

**손으로 적은 날짜에 굳이 생성기를 둔 이유는 시간대입니다.** 이 파일에서 틀리기 쉬운 것은 날짜가 아니라 환산입니다.

| 현지 발표 | 한국 시각 |
|---|---|
| 08:30 ET (CPI·고용·GDP·PCE·소매판매) | **같은 날** 21:30(서머타임) / 22:30 |
| 10:00 ET (ISM) | 같은 날 23:00 / **다음 날** 00:00 |
| 14:00 ET (FOMC) | **다음 날** 03:00 / 04:00 |

하나의 오프셋을 통째로 적용하면 이 중 어느 하나는 반드시 하루가 어긋납니다. 그래서 환산은 IANA 시간대 규칙(`America/New_York`)에 맡기고, 서머타임 경계(2026-11-01) 양쪽에서 실제로 값이 갈리는지를 배치와 회귀 테스트가 각각 확인합니다.

**공표된 날짜와 규칙으로 잡은 날짜를 구별합니다.** ISM 달력은 로그인을 요구해 열지 못했고, 산업통상부 수출입 동향은 공표 일정표가 아니라 "매월 1일" 관례입니다. 이 둘은 `dateBasis: "rule"`로 표시하고 화면에도 `예정` 표시를 답니다 — 확정 일정과 같은 모양으로 보여주면 실제로 하루 밀렸을 때 왜 틀렸는지 알 수 없습니다. 미 노동통계국(bls.gov)은 자동 조회를 차단(HTTP 403)해 BLS 일정을 그대로 싣는 페이지에서 확인했고, 갱신할 때는 브라우저로 직접 열어야 합니다.

**커버리지가 기관마다 다릅니다.** FOMC는 2027년까지 공표됐지만 한국은행 금통위는 2026-11-26까지입니다(2027년 페이지가 빈 표로 옵니다). 그래서 `coverage`에 계열별 마지막 날짜를, `coverageLabels`에 사람이 읽을 이름을 적고, 한쪽이 바닥나면 카드 하단에 그 사실을 적습니다 — 안 적으면 "요즘 금통위 일정이 왜 안 보이지"를 알아챌 방법이 없습니다. 커버리지는 일정 목록에서 자동으로 뽑지 않습니다. 마지막 일정은 언제나 마지막 일정이라, 자동 계산은 "언제까지 공표됐는지"를 알려주지 못합니다.

**만료는 반드시 시끄럽게 알립니다.** `coverageUntil`을 지났거나 남은 미래 일정이 없으면, 빈 카드나 "일정이 없습니다" 같은 일반 문구 대신 **갱신이 필요하다는 사실과 `coverageUntil` 날짜를 명시**합니다. 데이터가 낡아서 비어 있는 것인지 원래 일정이 없는 것인지 구별되지 않으면 그 카드는 신뢰를 잃습니다.

**시나리오 문구의 경계.** 일반적으로 알려진 파급 경로만 서술합니다. 매수·매도·비중 조정 같은 행동 지시, 읽는 사람의 보유 자산 언급, 어느 쪽이 될지에 대한 예측이나 확률은 넣지 않습니다. 금리 외 지표는 **양방향 서프라이즈**(예상보다 높게/낮게)를 두 시나리오로 씁니다. 회차마다 메커니즘이 달라지지 않으므로 지표 종류별로 같은 문구를 씁니다 — 회차별로 다르게 쓰면 있지도 않은 구체성을 지어내는 셈입니다. 이 금지 표현은 생성기와 회귀 테스트가 **양쪽에서** 검사합니다.

### 주별·월별 보기와 필터

컴팩트 카드와 확대 보기 모두 `가까운 순 · 주별 · 월별` 세 가지로 볼 수 있습니다. 고른 값은 모듈 스코프에 있어 확대 보기를 열고 닫거나 일정을 펼쳤다 접어도 유지됩니다.

**주는 월요일에 시작하고 묶음은 전부 한국 날짜 기준입니다.** 일요일 시작으로 하면 주말 이틀이 갈려 "이번 주 일정"이 둘로 나뉩니다. 달을 넘는 주는 라벨에 양쪽 달을 다 적습니다(`8월 31일~9월 6일`) — `8월 31일~6일`은 어느 달의 6일인지 알 수 없습니다. 지역 시간대가 끼어들면 자정 근처 일정이 옆 주로 새므로 UTC 자정으로 고정한 정수 연산만 씁니다.

**필터(국가·중요도)는 확대 보기에만 둡니다.** 컴팩트 카드는 "다음에 무엇이 오는지"를 빠뜨리지 않고 보여주는 것이 역할이라, 거기서 조용히 걸러지면 안 온다고 착각하게 됩니다. 필터 결과가 0건이면 **조건 때문이라는 것과 남은 전체 건수**를 밝힙니다 — 만료 안내와 같은 문구로 뭉치면 데이터가 낡은 것인지 조건이 좁은 것인지 구별되지 않습니다.

카드 높이는 상단 3열 행을 늘리지 않도록 묶여 있고, 넘치면 카드 안에서 스크롤합니다(접기 토글 없음). 실측으로 상단 행 높이 327px은 총자산 카드가 정하고, 일정 카드는 보기 전환 줄을 더한 뒤에도 236px입니다. 회귀 테스트가 이 값을 못 박습니다 — 카드의 `scrollHeight`만 보면 늘어난 높이라 행 높이와 늘 같아, 카드가 행을 밀어 올려도 통과해 버립니다.

### 확인이 필요한 항목

카드 하나를 통째로 쓰지 않고 총자산 카드 아래 한 줄로 붙이며, 항목이 없으면 그 줄을 숨깁니다. 다만 **내용을 없애지는 않습니다** — 이 줄은 `시세 확인 불가 N건`, `매입 정보가 없어 손익에서 빠진 N건`처럼 화면의 숫자가 왜 불완전한지 설명하는 신뢰 장치이고, 사라지면 총자산이 왜 어긋나는지 알 방법이 없어집니다.

`asset-input.html`의 `--content-max`(38rem)는 폼의 상한입니다. 화면이 넓다고 입력 필드를 끝까지 늘이면 한 줄에서 눈이 왕복하는 거리가 길어져 오히려 쓰기 나빠집니다.

### 평가손익과 평가차액

금융자산은 `평가손익`, 실물자산은 `평가차액`으로 **따로** 냅니다. 합산하면 부동산이 손익의 98.7%를 차지해 주식 손익이 묻히는 데다, 부동산의 그 차액은 시장이 매긴 값이 아니라 사용자가 입력한 추정가와 매입가의 차이라 숫자의 성격이 다릅니다.

`lib/valuation.js`를 두 화면이 공유합니다. 평가 로직이 화면마다 따로 있으면 한쪽만 고쳐지는 순간 같은 자산이 다른 금액으로 보이는데, 총자산은 사용자가 가장 먼저 보는 숫자라 그런 불일치는 곧바로 신뢰 문제가 됩니다. 두 파일 모두 클래식 스크립트로 두고 전역(`window.Valuation`)으로 노출합니다 — ES 모듈로 바꾸면 `asset-input.html`이 의존하는 전역 스코프와 회귀 테스트의 `page.evaluate` 경로가 함께 깨집니다.

### 금융자산 · 실물자산 분리

부동산 하나가 90%를 넘는 포트폴리오에서는 한 그래프에 다 넣으면 나머지 자산군이 전부 실 가닥으로 뭉쳐 읽히지 않습니다(실측: 부동산 91.1%, 예적금 1,200만원·주식 892만원·금 977만원이 화면에서 구분 불가). 그래서 총자산 카드에 `금융자산 / 실물자산`을 분해해 적고, 구성 카드는 둘을 전환합니다.

`자산 유형 / 원금 보장 / 통화` 3축은 **금융자산 쪽에만** 둡니다 — 실물자산은 전부 시가 변동이고 전부 원화라 나머지 두 축이 한 덩어리가 됩니다.

**포트폴리오 요약 축은 금융자산 기준으로 고정**합니다(구성 카드의 전환과 연동하지 않습니다). 원금 보장·현금화·집중도는 투자 판단과 연결된 지표인데, 실거주 아파트를 팔아 집중도를 낮출 수는 없습니다. 부동산을 넣으면 세 축이 전부 부동산 하나에 끌려갑니다(실측: 2% / 4% / 95% → 금융자산 기준 37% / 36% / 66%).

**성장 가능성 축은 내렸습니다.** 금융자산 기준으로 재보니 위험자산('높음') 비중과 같은 값이 나옵니다 — 검증 포트폴리오에서 둘 다 53.8%로 완전히 일치했고, 무작위 표본 5,000개 중 29%에서 소수점까지 동일했습니다(r=0.80). 금융자산 6개 자산군 중 '중간'이 나올 수 있는 건 혼합형 펀드와 회사채뿐이고, 현금·예적금은 항상 '낮음', 주식·ETF·가상자산은 항상 '높음'이라 그 둘이 없으면 정의상 같은 숫자입니다. 분류 자체(`row.growth`)는 남겨 뒀습니다 — 업종 데이터로 주식 내부를 세분화하면 독립적인 정보를 갖게 되고, 그때 `meters()`에 한 줄 추가하면 됩니다.

### 구성 그래프

**도넛**입니다. 한동안 가로 누적 막대였는데, 그렇게 바꾼 이유는 부동산이 91%를 차지해 나머지 자산군이 실 가닥으로 뭉쳤던 것이고 그 문제는 금융자산·실물자산 분리로 해결됐습니다. 금융자산 안에서는 54% / 15% / 10% / 8% / 7% / 6%처럼 고르게 분포합니다.

도넛의 나머지 문제는 크기가 아니라 색이었습니다. 조각이 전부 초록 계열 그라데이션이라 서로 구분되지 않았습니다. 지금은 색각 이상 시뮬레이션을 포함한 검증을 통과한 순서를 씁니다(다크 표면 `#141b2b` 기준 인접쌍 최저 CVD ΔE 8.4, 일반 시야 최저 19.3, 전부 대비 3:1 이상). **빨강·초록은 뺐습니다** — 손익에만 색을 쓰기로 했는데 구성 그래프에 그 두 색이 있으면 비중이 큰 게 좋거나 나쁜 것처럼 읽힙니다. 순서나 색을 바꾸면 다시 검증해야 합니다.

도넛 조각과 범례 행 **모두** 해당 자산군 화면으로 이동합니다. 다만 작은 조각은 누르기 어려우므로 금액·비율이 함께 적힌 범례 행이 주 진입점이고, 행 높이는 44px 이상입니다.

요약 축은 중앙(50%)에서 값까지만 채운 **선**입니다. 왼쪽 끝부터 채우면 "가득 채울수록 좋다"로 읽히는데, 집중도 79%가 꽉 차 있으면 성취처럼 보입니다. 중앙 기준이면 선이라 잘 보이면서도 어느 쪽으로 치우쳤는지만 드러납니다.

### 세션 스키마가 바뀌었을 때

`SESSION_SCHEMA`가 올라가도 **사용자가 입력한 자산을 지우지 않습니다.** 예전에는 스키마가 다르면 곧바로 빈 세션을 덮어썼는데, 그건 앱을 업데이트하는 순간 자산 20건을 입력해 둔 사용자의 기록을 실제로 삭제하고 화면에는 "자산이 없어요"를 띄우는 코드였습니다.

지금은 원본을 `assetInput.session.backup.v<이전스키마>`로 남기고, 자산 레코드(`{group, fields, autoFields}`)를 그대로 옮겨옵니다. 홈 화면은 예전 형식에서 읽어온 세션을 **절대 덮어쓰지 않습니다**(스냅샷 몇 건보다 원본 보존이 우선). 화면에서도 `자산 0건`과 `예전 형식이라 못 읽음`을 구분해 안내합니다.

홈 화면의 스냅샷(`session.snapshots`)은 `asOf`가 바뀐 것을 감지한 첫 실행에만 `{date, total}` 1건을 쌓습니다. 앱을 연 시각 기준으로 하면 하루에 여러 번 열 때 중복되고 며칠 안 열면 구멍이 생깁니다. 기록이 없는 날짜 구간은 직선으로 잇지 않고 점선으로 끊은 뒤 비어 있는 일수를 표기합니다 — 매끄럽게 이으면 그 기간에 자산이 그렇게 변한 것처럼 보이고, 그건 사실이 아닙니다.

종목 데이터는 앱 런타임과 분리된 1회성 빌드로 갱신합니다.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-tickers.txt
$env:DATA_GO_KR_KEY="..."; $env:DART_API_KEY="..."; node scripts/build-tickers.mjs
```

생성 파일은 `data/tickers-kr.json`, `data/tickers-us.json`, `data/tickers-global.json`입니다. 국내 주식 목록은 KIND(한국거래소) 페이지를 우선 사용하고, 너무 적게 나오면 공공데이터포털 금융위원회_KRX상장종목정보로 재시도합니다. 국내 ETF·ETN은 금융위원회_증권상품시세정보에서 받습니다(예전엔 `pykrx`·네이버 금융 스크래핑을 썼습니다 — 출처 신뢰성 때문에 공식 API로 교체했습니다). `DART_API_KEY`가 있으면 OPEN DART(금융감독원)에서 종목별 업종을 함께 채워 넣습니다 — 없어도 빌드는 진행되고 업종만 비어 있습니다. 빌드는 한국 2,500건, 미국 5,000건, 글로벌 1,000건의 최소 건수를 검증하고 스코프별 전체 건수와 ETF·ETN 건수를 출력합니다. 수집 실패나 최소 건수 미달은 종료 코드 `1`과 콘솔 오류로 표시됩니다.

이 빌드는 `.github/workflows/tickers.yml`이 **매주 일요일 05:00 KST**에 자동 실행합니다. 매일 도는 시세 배치와 일부러 분리해 뒀습니다 — 종목 목록 갱신은 DART 업종 조회만 2,700회가 넘어 훨씬 잘 깨지는데, 한 잡에 묶으면 업종 조회가 실패한 날 시세까지 멈춥니다.

### 업종 (KSIC)

DART 기업개황의 `induty_code`는 한국표준산업분류(KSIC) 코드를 3~5자리로 줍니다(삼성전자 `264`, 현대차 `30121`, NAVER `63120`). 세분류라 그대로 쓰면 도넛 조각이 수백 개로 쪼개지고, 숫자를 화면에 띄울 수도 없습니다. 그래서 종목마다 세 필드를 저장합니다.

| 필드 | 예시 | 용도 |
|---|---|---|
| `sec` | `"264"` | DART 원본 세분류 코드. 상세 화면·재분류용으로 보존 |
| `secDiv` | `"26"` | 앞 2자리 중분류 코드 |
| `secDivName` | `"전자·통신장비"` | 중분류 한글명. 홈 화면 산업 축(도넛)이 쓰는 값 |

중분류 표는 `scripts/lib/ksic.mjs`에 있습니다(KSIC 10차 기준, 전 중분류 수록 — 실측 데이터의 348개 세분류 코드가 전부 알려진 중분류로 풀립니다). **세분류(3~5자리) 한글명은 싣지 않았습니다** — 롱테일이라 상위 45개 코드가 절반도 덮지 못하는데, 공식 분류표 없이 손으로 적으면 틀린 업종명을 그럴듯하게 박아 넣게 됩니다. 세분류 이름이 필요해지면 통계청 한국표준산업분류 표를 받아 `ksic.mjs`에 추가하고 `resolveSector`가 그걸 우선 쓰게 하면 됩니다(호출부는 그대로 둬도 됩니다).

## 국내 시세 배치 (data/quotes.json)

국내 주식·ETF·ETN 시세, 금·환율(및 있으면 은)을 받아 `data/quotes.json`을 굽습니다. 앱은 이 정적 파일을 fetch만 할 뿐 API 키를 전혀 갖고 있지 않습니다.

### ⛔ 2026-09-05부터 수동입니다

**data.go.kr이 GitHub Actions 러너를 TCP 층에서 막습니다.** 08-30 이후 13회 연속 실패했고 사유는 전부 같았습니다 — `fetch failed (UND_ERR_CONNECT_TIMEOUT)`. 러너에서 잰 값(실행 [33937391146](https://github.com/oliversong25-lang/mon2/actions/runs/33937391146)):

| 호스트 | TCP 443 (한도 60초) |
|---|---|
| `apis.data.go.kr` | **타임아웃 60.5초** |
| `www.data.go.kr` | **타임아웃 60.2초** |
| `opendart.fss.or.kr` | 0.4초 OK |
| `ecos.bok.or.kr` | 0.3초 OK |
| `www.koreaexim.go.kr` | 0.5초 OK |

**미확인 (열린 항목)**: 위 표의 `www.koreaexim.go.kr`은 수출입은행 **홈페이지** 주소(39.115.136.135)입니다. 환율 API가 실제로 쓰는 호스트는 `oapi.koreaexim.go.kr`이고 수출입은행이 공지한 **API 호출용 IP는 39.115.136.167**로 다릅니다. **러너가 `oapi`에 닿는지는 아직 확인하지 않았습니다.** 예약 실행을 되살릴 때 함께 확인해야 합니다 — data.go.kr만 뚫려도 환율에서 다시 막힐 수 있습니다.

**타임아웃을 늘려서 될 일이 아닙니다** — 60초에도 연결이 성립하지 않습니다. DNS는 정상이고 올바른 IP(27.101.236.63)를 돌려줍니다. 같은 러너에서 다른 한국 정부 호스트는 전부 붙으므로 "러너가 한국에 못 간다"가 아니라 **data.go.kr 두 호스트만** 막힌 것입니다. 같은 배치가 국내 IP에서는 그대로 성공합니다.

매일 빨갛게 실패하도록 두지 않았습니다 — 실패 알림을 무시하는 습관이 들면 정작 다른 것이 깨졌을 때 놓칩니다. `quotes.yml`의 `schedule` 블록을 주석으로 막았고 `workflow_dispatch`는 남겨 뒀습니다.

**다시 켜는 조건**: 러너에서 `apis.data.go.kr`에 TCP 연결이 성립하면. `workflow_dispatch`로 한 번 돌려 확인하고, `quotes.yml`의 `schedule` 주석만 풀면 원래대로 돌아갑니다. 인프라 결정(국내 서버·자체 호스팅 러너)은 미뤄 둔 상태입니다.

`tickers.yml`은 일부러 그대로 뒀습니다. 같은 호스트를 쓰지만 주 1회(일요일)라 08-29 이후 안 돌았고, 그 실행이 차단 범위가 저장소 전체인지 이 배치만인지 알려줍니다. **그 실패는 예상된 것입니다.**

### 손으로 돌리는 법 (국내 IP에서)

`.env.example`을 `.env`로 복사해 키를 채운 뒤, 저장소 루트에서:

```powershell
npm run quotes:local
```

`.env`는 `.gitignore`가 막습니다. 이 스크립트는 `node --env-file=.env`를 쓰므로 **Node 20.6 이상**이 필요하고, `.env`가 없으면 조용히 넘어가지 않고 `.env: not found`로 즉시 멈춥니다 — 키 없이 도는 것보다 낫습니다.

**이 플래그는 CI에 닿지 않습니다.** 워크플로 4개는 전부 `node scripts/*.mjs`를 직접 부르고 npm 스크립트를 거치지 않으며, CI는 `node-version: 20`에 고정돼 있습니다. 기존 `build:quotes` 스크립트도 그대로 뒀습니다.

배치가 성공하면 결과를 **손으로 커밋합니다**:

```powershell
git add data/quotes.json
git commit -m "data: refresh domestic quotes (manual)"
git push
```

자동 커밋을 붙이지 않았습니다. 이 배치는 여러 소스를 순서대로 받는데(주식·ETF·ETN·금·환율·가상자산), 중간에 하나가 실패하면 반쯤 채워진 파일이 남을 수 있습니다. 예약 실행 때는 실패 시 커밋 스텝에 도달하지 못하는 구조가 그걸 막아 줬지만, 손으로 돌릴 때는 사람이 로그를 보고 판단하는 편이 안전합니다 — 반쯤 채워진 `quotes.json`을 밀어 넣으면 앱이 구조적으로는 멀쩡한 채 틀린 총자산을 보여줍니다.

**14:00인 이유**: 금융위원회_주식시세정보는 기준일 *다음 영업일 13시 이후*에 갱신됩니다. 06:00에 돌리면 전일 데이터가 아직 없어 소급 조회가 그 전 영업일로 내려가고, 결과적으로 **항상 2영업일 전 종가**를 쓰게 됩니다(실측: 8/11 06:38 실행이 8/10 대신 8/7 종가를 가져옴). 제공 시각보다 1시간 뒤에 돌려 전일 종가를 잡습니다.

배치는 데이터가 있는 날짜를 찾을 때까지 소급 조회하므로 제공이 밀려도 실패하지 않고 조용히 하루 뒤처진 값을 씁니다. 그래서 기준일이 직전 영업일보다 이르면 **경고**를 남깁니다 — 공휴일이면 정상이지만 매일 반복되면 배치 시각을 다시 봐야 한다는 신호입니다. 공휴일을 알 수 없으므로 실패로 처리하지는 않습니다.

```powershell
# .env를 쓰는 쪽을 권합니다 (위 참고). 환경변수를 직접 넣어도 됩니다:
$env:DATA_GO_KR_KEY="..."; $env:KOREAEXIM_AUTH_KEY="..."; node scripts/build-quotes.mjs
```

**필요한 키 4곳** (전부 무료 · 제가 대신 발급받을 수 없어 직접 신청해야 합니다):

| 환경변수 | 발급처 | 용도 |
|---|---|---|
| `DATA_GO_KR_KEY` | [data.go.kr](https://www.data.go.kr) — 금융위원회_주식시세정보(15094808)·증권상품시세정보(15094806)·일반상품시세정보(15094805)·KRX상장종목정보(15094775) 활용신청 | 국내 주식·ETF·ETN·금 시세, 종목 목록 |
| `KOREAEXIM_AUTH_KEY` | [한국수출입은행 Open API](https://www.koreaexim.go.kr/ir/HPHKIR020M01) | 환율 |
| `COINGECKO_API_KEY` | [CoinGecko](https://www.coingecko.com/en/api) | 가상자산 원화 시세 |
| `DART_API_KEY` | [OPEN DART](https://opendart.fss.or.kr) | 종목 업종 (선택 — 없으면 업종 없이 진행). `build-tickers.mjs`만 씁니다 |
| `ECOS_AUTH_KEY` + `ECOS_SILVER_STAT_CODE` | [한국은행 ECOS](https://ecos.bok.or.kr/api) | 은 시세 (선택 — 통계표코드는 키 발급 후 `StatisticItemList`로 확인 필요, 없으면 은 없이 진행) |

GitHub Actions에서 쓰려면 위 값들을 저장소 Settings → Secrets and variables → Actions에 같은 이름으로 등록하세요. 워크플로별로 쓰는 키가 다릅니다.

| 워크플로 | 주기 | 쓰는 Secret |
|---|---|---|
| `quotes.yml` (시세) | **수동 (예약 중지, 2026-09-05)** | `DATA_GO_KR_KEY`, `KOREAEXIM_AUTH_KEY`, `COINGECKO_API_KEY`, `ECOS_AUTH_KEY`, `ECOS_SILVER_STAT_CODE` |
| `tickers.yml` (종목 목록·업종) | 매주 일요일 05:00 KST | `DATA_GO_KR_KEY`, `DART_API_KEY` |
| `daily-rates.yml` (일간 금리) | 매일 2회 | `ECOS_AUTH_KEY` |
| `indicators.yml` (OECD 지표) | 매일 15:00 KST | 없음 |

**이 표가 빠뜨리면 조용히 망가집니다.** `daily-rates.yml`이 여기 없던 동안 `ECOS_AUTH_KEY`가 Secret에 등록되지 않은 채로 지나갔고, 배치는 국내 금리를 통째로 빠뜨린 채 매일 "성공"으로 끝났습니다(부분 실패를 허용하는 구조). 워크플로를 추가하면 이 표도 함께 고치세요.

`quotes.json` 형식:
```json
{
  "asOf": "2026-08-06T00:00:00+09:00",
  "sources": { "equity": "금융위원회_주식시세정보", "fx": "한국수출입은행" },
  "quotes": { "005930": { "price": 73400, "currency": "KRW" } },
  "rates": { "USD": 1380.5 },
  "commodities": { "goldPerGram": 151200 }
}
```
`asOf`는 배치 실행 시각이 아니라 실제 시세 기준일입니다(환율 조회는 주식 기준일부터 역순으로 탐색해서 시작하고, 그래도 갈리면 더 이른 쪽을 씀). 시세를 못 받은 종목은 앱에서 "시세 확인 불가"로 명시되고 평가금액 계산에서 빠집니다 — 조용히 사라지지 않습니다.

### 가상자산 시세 (CoinGecko)

**유료 서비스로 전환할 때 반드시 CoinGecko Analyst 이상 플랜으로 올려야 합니다.** Demo 플랜에는 상업 라이선스가 없습니다. 전환 시 `COINGECKO_PLAN=pro`로 바꾸면 호스트와 인증 헤더가 함께 바뀝니다(코드 수정 불필요).

국내 거래소를 먼저 검토했으나 업비트 Open API 이용약관 제5조(데이터 저작권은 회사에 있고 무단 사용·변경 금지)와 제4조 2항 ③(서비스를 이용하는 응용프로그램을 유상으로 양도·배포·이용허락하면 이용 제한)이 "시세를 받아 `quotes.json`에 구워 배포하는" 이 구조와 정면으로 걸리는데, 유료 상업 라이선스를 살 경로 자체가 없었습니다. CoinGecko는 유료 플랜에 상업 라이선스가 포함돼 키만 바꾸면 합법화됩니다 — yfinance·증권사 API를 뺐던 기준(합법화 경로의 유무)과 같은 판단입니다.

**코인 ID 매핑**: 자산 입력이 저장하는 코드는 심볼(`BTC`)이고 CoinGecko ID는 다릅니다(`bitcoin`). `lib/crypto.mjs`의 `COINGECKO_IDS` 표로만 매핑하고, 표에 없는 심볼은 매칭 실패로 세어 출력합니다. 심볼로 ID를 자동 추론(`coins/list` 검색 등)하지 않는 이유는 같은 심볼을 쓰는 코인이 수백 개라 자동 매칭이 조용히 엉뚱한 코인의 시세를 물어오기 때문입니다 — 종목코드 `A` 접두어와 같은 종류의 사고이고, 그때는 값이 비어서 티라도 났지만 이건 그럴듯한 숫자가 들어와 더 나쁩니다.

화면에는 `Data provided by CoinGecko`(약관 요구 출처 표기)와 **국제 시세 기준**임을 함께 밝힙니다. CoinGecko는 국제 평균가라 국내 거래소 체결가와 김치 프리미엄만큼 차이가 날 수 있고, 사용자가 업비트에서 샀다면 평가손익이 실제와 어긋납니다.

### 종목코드 형식

`data.go.kr`의 두 엔드포인트는 같은 종목에 다른 코드 형식을 씁니다 — KRX상장종목정보는 `srtnCd`에 `A` 접두어를 붙이고(`"A900110"`), 주식시세정보는 안 붙입니다(`"900110"`). `lib/data-go-kr.mjs`의 `normalizeKrCode(srtnCd, isinCd)`가 모든 코드 추출 지점에서 이걸 흡수합니다 — 표준 국내 ISIN(`KR7` + 6자리 + 검사숫자)이면 ISIN에서 6자리를 복원하고, 아니면(외국적 상장사 등) 접두어만 제거합니다. `data/tickers-*.json`이나 사용자 자산 레코드에 `A` 접두어가 남아있으면 안 됩니다.

### 산출물 검증

`build-quotes.mjs`는 쓰기 전에 검증하고, 실패하면 **기존 `data/quotes.json`을 건드리지 않습니다**(임시 파일에 먼저 쓰고 통과해야 원자적으로 교체):

- 실패(종료 코드 1, 기존 파일 보존): 주식·ETF·ETN **각각의** 시세 매칭률 90% 미만, 환율 통화 10개 미만, 금 시세 누락, `asOf`가 7일 이상 과거, 전체 시세 3,000건 미만
- 실패: 가상자산 시세 0건 **이면서 보존할 직전 값도 없을 때만** (CoinGecko는 잘못된 ID에도 HTTP 200 + 빈 객체 `{}`를 돌려주므로 status만 보면 성공으로 읽힙니다 — 명시적으로 끊습니다)
- 경고(로그만, 게시는 진행): 직전 산출물 대비 시세 건수 ±30% 초과 변동, 종목 목록에 없는 시세 코드 300건 초과, 가상자산 일부 종목 미확보(해당 자산만 앱에서 "시세 확인 불가")

매칭률의 분자는 **종목 목록과 시세 코드의 교집합**입니다. 시세 API가 돌려준 행 수를 그대로 분자로 쓰면 목록에 없는 코드까지 세어 100%를 넘고(실측 104.1%), 그보다 나쁜 건 코드 조인이 깨져 교집합이 0이 돼도 행 수는 그대로라 검증이 통과해 버린다는 점입니다 — 접두어 버그를 잡으려고 넣은 검증이 정확히 그 버그를 통과시켰습니다. 이 케이스는 `test-quotes-mock.mjs`에 회귀 테스트로 고정돼 있습니다.

`build-tickers.mjs`는 DART 업종 매칭률이 **80% 미만이면 실패**합니다(예전엔 경고만 하고 진행 — 그래서 63%가 비어 있는 산출물이 "성공"으로 커밋됐습니다). 미확보분은 `corpCode 없음`과 `호출 실패`로 나눠 출력하므로 원인을 바로 구분할 수 있습니다.

DART는 분당 호출 한도가 있고, 넘기면 이후 요청을 전부 `020`으로 돌려줍니다. 예전 코드는 `status !== "000"`을 전부 null로 뭉개서 이 차단을 "업종 없는 종목"과 구별하지 못했고, 그 결과 2,758건 중 **정확히 앞 1,010건만** 업종이 붙고 나머지가 통째로 비었습니다. 지금은 `lib/dart.mjs`가 분당 900회로 스스로 유량을 제한하고, `013`(데이터 없음)만 정상적인 빈 결과로 취급하며, `020`은 재시도 후 실패로 보고하고, 인증키 오류(`010`·`011`·`012`·`901`)는 즉시 예외로 올립니다.

### 전일 종가와 총자산 변화

응답에는 **전일 종가 필드가 없습니다.** 있는 것은 종가(`clpr`)와 전일 대비(`vs`)뿐이라 빼서 만듭니다: `prevClose = clpr - vs`.

문제는 `vs`가 정말 "전일 대비"인지를 문서로만 믿어야 한다는 점입니다. 이 프로젝트에서 필드명을 잘못 짚으면 예외가 아니라 **조용한 0**으로 끝납니다(전일 대비가 0이면 변동 없음으로 보입니다). 그래서 문서를 믿는 대신 **응답 스스로 증명하게** 합니다. 같은 행에 등락률(`fltRt`)이 함께 오므로 이 항등식이 성립해야 합니다.

```
vs / (clpr - vs) * 100  ≈  fltRt
```

한 번 눈으로 확인하는 것보다 강합니다 — 매 실행마다 의미를 검증하고, 제공처가 나중에 조용히 의미를 바꿔도 잡힙니다. 어긋나면 **배치를 멈추고 원본 행을 통째로 찍습니다.** 기본값으로 때우거나 0을 쓰지 않습니다.

허용 오차는 절대 0.02%p 또는 상대 1% 중 느슨한 쪽입니다. `fltRt`가 소수 둘째 자리로 반올림돼 오기 때문인데(응답이 `".7"` `"-.24"` 같은 모양입니다), 2026-08-13 실측으로 네 출처의 최대 오차가 정확히 그 반올림 경계였습니다 — 주식 0.00495 · ETF 0.00500 · ETN 0.00500 · 금 0.00059 %p. 측정 최대치의 4배를 허용치로 두어 반올림은 넉넉히 덮고, 필드 의미가 달라지는 경우(오차가 자릿수로 벌어짐)는 확실히 잡습니다.

**총자산 변화는 지금 들고 있는 자산을 전일 종가로 다시 평가해 냅니다.** 예전에는 이 브라우저가 마지막으로 본 스냅샷과 비교했는데, 그러면 며칠 만에 열었을 때 며칠치 변화가 나오고, 스냅샷이 총액을 담고 있어 **자산을 추가하면 그 금액이 통째로 변동으로 잡혔습니다**. 지금은 자산을 더하거나 빼면 양쪽이 같이 움직이므로 변화로 잡히지 않고, 남는 것은 가격뿐입니다.

전일 값이 없는 자산은 **0으로 때우지 않고 빼고 건수를 밝힙니다.** 가상자산(코인게코가 전일 종가를 주지 않음)과 외화 자산(전일 환율 없음), 은(ECOS)이 여기 해당합니다. 시세가 붙는 자산이 하나도 안 남으면 0원이라고 적지 않고 계산하지 못했다고 말합니다.

스냅샷 이력은 그대로 둡니다 — 추이 차트가 씁니다. 08-07에서 08-12로 뛴 구간은 실제 기록이고 차트는 이미 그 간격을 점선으로 끊어 그립니다.

## 회귀 테스트

Playwright로 실제 클릭·키보드 입력을 재현해 검증합니다 (값을 JS로 주입하는 방식은 통과 근거로 쓰지 않습니다).

```powershell
npm install
npx playwright install chromium
npm test
```

각각 자체 정적 서버를 띄우는 독립 스크립트입니다. 개별 실행도 가능합니다.

- `npm run test:focus` — 렌더가 돌 때 포커스 중인 입력 필드를 잃지 않는지(커서 이탈 방지). 8개 자산군의 금액·수량 필드 클릭 후 포커스 유지, 연속 타이핑, 통화·시·도 변경 후 값 보존, 검토 화면 새로고침 유지 등.
- `npm run test:numeric` — `numeric:true` 필드(수량·지분율·금리 등)에 문자·한글이 섞여 들어가지 않는지. beforeinput 정제, 붙여넣기, 한글 IME 조합 커밋, 자산군별 소수점 자릿수 제한, 지분율 0~100 clamp.
- `npm run test:groups` — 펀드·채권·원자재·부동산 4개 자산군의 실클릭 E2E. 필드별 클릭·타이핑·삭제·재입력, 복수 등록·중복 방지, 통화 전환, 원자재 종류 전환, 시·도 연동, 건너뛰기/이전/이어하기, 새로고침 보존, 최종 검토 화면, 모바일 뷰포트.
- `npm run test:quotes-integration` — `data/quotes.json`이 실제로 화면에 반영되는지. MOCK에 한 번도 없던 종목(카카오)을 실제 검색·등록해 평가금액이 정상 계산되는지, 시세 없는 종목(현대차)은 "확인 불가"로 명시되고 조용히 사라지지 않는지, 기준일 표기가 `asOf` 기반인지, `quotes.json` 자체가 없어도(배치 실패/최초 상태) 앱이 죽지 않는지 확인합니다. 실제 커밋된 `data/tickers-kr.json`을 그대로 쓰고 테스트용 `quotes.json`만 임시로 얹었다 지웁니다.
- `npm run test:home` — 홈 화면과 자산군 탭. 자산을 JS로 주입하지 않고 입력 화면에서 실제로 클릭·타이핑해 5건(예적금·삼성전자·카카오·BTC·공동명의 부동산)을 등록한 뒤 홈으로 이동해, 총자산이 손으로 계산한 값과 일치하는지, 부동산 지분율 50%가 반영되는지, 구성 축 3개의 비중 합이 각각 100%인지, 손익 모집단과 총자산 모집단이 다르다는 것이 화면에 드러나는지, 도넛 조각·목록 행 클릭이 실제로 이동하는지, 모바일 폭 첫 화면에 총자산과 자산 구성이 함께 보이는지, 자산 0건·첫날·시세 로드 실패·기록 끊긴 구간이 정상 동작하는지 확인합니다. 이어서 **현금만 고른 뒤 주식·가상자산을 나중에 더하는 경로**를 실제 클릭으로 밟아, 검토 화면에 자산군 8개가 모두 있는지, 나중에 더한 자산이 홈 총자산과 해당 자산군 탭에 나오는지, 입력 도중 새로고침해도 단계와 값이 남는지, 체크를 풀어도 기록이 지워지지 않고 어디에 남는지 화면에 적히는지 확인합니다.
- `npm run test:quotes-mock` / `npm run test:tickers-mock` / `npm run test:indicators-mock` — `build-quotes.mjs`/`build-tickers.mjs`/`build-indicators.mjs`의 페이징·기준일 탐색·필드 매핑·DART corpCode zip 파싱·SDMX 관측 디코딩·실패 격리 로직을 실제 API 키 없이 mock fetch로 검증합니다. **실제 공공데이터 API 응답 필드명 자체를 검증하지는 않습니다** — 그건 실제 키로 첫 배치를 돌려봐야 확인됩니다(각 배치는 `[raw-sample]`로 응답의 원본 첫 행을 콘솔에 출력합니다).
- `npm run test:business-cycle` — 경기국면 데이터 계약과 두 화면. 내보낸 JSON이 국면 이름만 담고 한계를 빠뜨리지 않았는지, 판정 보류 주가 공식 국면을 내지 않는지, 홈 카드와 분석 탭이 국면과 **증거 품질을 함께** 보여주는지, 투자 판단 어휘가 없는지, 경제지표 탭이 그대로 남아 있는지 확인합니다.
- `npm run test:auth` — 로그인·회원가입·로그아웃, 계정별 세션 격리, 오프라인 대기열과 재연결 재시도.
- `npm run test:rls` — 실제 Supabase 두 계정으로 행 격리 검증(위 "로그인과 계정별 저장" 참고). 환경 변수가 없으면 성공으로 가장하지 않고 실패합니다.

### 배치 회귀 테스트를 건드릴 때 조심할 것

여기서 실제로 데이터를 잃은 적이 있어 적어 둡니다.

- **테스트는 커밋된 실데이터 위에서 돕니다.** Playwright route 핸들러에서 던진 미포착 예외가 `finally`보다 먼저 프로세스를 죽여, 4,402건짜리 `data/quotes.json`이 2건짜리 픽스처인 채로 남았습니다. 지금은 `process.on("exit")`·`uncaughtException`·`unhandledRejection`에 동기 복원 훅이 달려 있습니다 — 새 배치 테스트를 쓸 때도 같은 훅을 답니다.
- **배치 완료를 고정 시간으로 기다리지 않습니다.** 재시도가 붙은 실패 경로는 테스트 정리 코드보다 늦게 끝나면서 산출물을 덮어쓰고 종료 코드를 뒤집습니다. `globalThis.__quotesBatchRuns`·`__indicatorsBatchRuns` 완료 카운터가 올라가는 것을 폴링합니다.
- **`localStorage.clear()`는 더 이상 깨끗한 시작이 아닙니다.** Supabase 인증이 붙은 뒤로 mock remote가 `sessionStorage`에 남습니다. `scripts/lib/test-auth.mjs`의 `seedSession()`·`restoreRegistered()`와 clear 오버라이드를 씁니다.
- **도넛 조각 클릭은 3시 방향에서 합니다.** `fill="none"`이라 칠해진 호만 히트되고, 12시 방향은 마지막 조각이 첫 조각을 덮습니다.

## 작업 이력과 남은 일

**[`WORKLOG.md`](WORKLOG.md)에 있습니다.** 역할을 이렇게 나눕니다 — 이 README는 설치·실행·구조와 **왜 이 구조인지**를 설명하고(오래 유지되는 내용), `WORKLOG.md`는 날짜별 변경·검증·인수인계를 기록합니다(계속 쌓이는 내용).
